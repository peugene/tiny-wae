---
id: l0-04.2
titre: CLI report (agrégats, ratios, SCL) + contact-sheet --first-last — testés sur fixtures
effort: M
categorie: pipeline
phase: O2
depends_on: [l0-03.6]   # étend contact-sheet ; manifests/fixtures par transitivité
parent: l0-04
---

# [l0-04.2] — Report et planche first/last

## Objectif

L'instrument de la recette — donc TESTÉ sur des comptes connus (la recette ne peut pas
être auto-certifiée par un composant non vérifié). Livre aussi la **commande de contrôle
de complétude** que la fiche humaine l0-04.H exécute (critère 2 de l'oracle du chapeau).

## Périmètre (fichiers nommés)

- `src/tiny_wae/core/report.py` (agrégation PURE sur les données de l'API manifests) +
  `src/tiny_wae/cli/report.py` : `report [--out <path>]` (défaut `<data_root>/report.md`).
  Par site, **dans cet ordre** (décision E-a) :
  - **Colonne de conservation** : `found_stac`, `skipped_scene_cloud`, `off_tile`,
    `found_tile`, puis `ingested`, `rejected_clouds`, `rejected_invalid`,
    **`rejected_nodata`**, `failed`, `skipped` — avec une **colonne de verdict
    `conservation: OK/ROUGE`** par site (les deux invariants du chapeau l0-02).
  - **`failed_pct` = failed / found_tile** (le seul critère à seuil : ≤ 1 %).
  - ⭐ **Colonne `integrite: OK/ROUGE`** (critère **O3** de la recette — chapeau l0-04
    et `lot-0-sites.md` §5 ; il n'avait aucun instrument — exactement le défaut que
    l'arbitrage n°3 vient de réparer pour la garde nodata) : **sans relire les
    rasters**, à partir des seuls manifestes —
    `files` contient bien les 3 noms attendus · `content_hashes` porte une entrée par
    fichier · `grid_hash` == celui de la grille **courante** du site (détecte les chips
    orphelins d'une correction de coordonnées) · `chip_nodata_pct < chip_nodata_pct_max`.
    Un `ingested` qui manque à l'un de ces points met le site en ROUGE, l'item est nommé.
  - **Publiés sans seuil** : `off_tile`, `skipped_scene_cloud`, et le **ratio
    `ingested / found_tile` avec son dénominateur affiché**. Section « pires cas en
    tête » triée sur ce ratio, avec une phrase d'explication automatique factuelle
    (« multi-tuiles : off_tile=317/636 » ou « nuages : rejected_clouds=…/… »).
  - Agrégat `scl_class_counts` (classes 2 et 11 mises en avant — instrument V3),
    `assets_read` agrégé, période couverte, et **volumes** : `bytes_downloaded` **avec sa
    mention de portée** (« STAC seul — les lectures d'assets par GDAL ne sont pas
    instrumentables depuis Python », décision E-d) + octets **stockés** (somme des tailles
    de fichiers listés au manifeste — champ `files` + `bytes_written` à porter par
    l0-03.2). Le volume réellement **transféré** en campagne est **hors instrument** : à
    mesurer au niveau système par l'humain (l0-04.H), ou à déclarer non mesuré.
  Aucune « cohérence statistique » (supprimée en E-a : non calculable — `cloud_pct`
  n'existe pas pour les items non instruits — et circulaire là où elle l'est).
  Sortie Markdown, logs STDERR.
- `cli/contact_sheet.py` : ajout du mode `--first-last` (premier et dernier chip
  `ingested` par site, 2 imagettes par case) — extension actée en l0-03.6.
- ⭐ **`report --check-completeness --sites A01,C07,…`** (critère 2 de l'oracle du chapeau)
  — **arbitrage n°2 : comparaison d'ENSEMBLES, pas de sommes de compteurs.** Pour chaque
  site : `item_ids_for_site(site)` (ensemble exact issu des manifestes, l0-03.2) vs les
  ids d'une requête `/search` paginée reproduisant **LES TROIS filtres du pipeline** :
  même bbox, même fenêtre, `grid:code` == `reference_tile`, **ET
  `eo:cloud_cover < scene_cloud_max`**.
  ⚠ **Ce troisième filtre est indispensable** : `item_ids_for_site` rend les items
  *instruits* (`found_tile`), or le pré-filtre scène de l0-02.1 en écarte
  `skipped_scene_cloud` **avant** l'ingestion. L'omettre rendrait le contrôle rouge dès
  qu'une scène dépasse 95 % de nuages — c'est-à-dire en permanence sur Hinkley ou
  Sizewell. **Règle générale : la requête témoin reproduit TOUS les filtres appliqués en
  amont du comparant, ni plus ni moins.**
  Sortie : les ids **manquants** et **en trop**, nommés. Tolérance 0.
  ⚠ **Ne JAMAIS comparer `found_stac` agrégé** : en régime permanent (update quotidien,
  marge 3 j) les runs se recouvrent et la somme sur-compte → le contrôle serait rouge en
  permanence, donc ignoré. Le comparant est le **`found_tile` effectif** (les manifestes),
  d'où le filtre tuile côté requête témoin. Option réseau, hors `just check`.

## ⚠ Ancrage dans le code réel (22/08/2026, avant dispatch — run N7)

Vérifié sur `develop` après le merge complet de N6. Cette fiche part **en parallèle de
l0-06.2** : fichiers disjoints, aucun contrat partagé en cours de création.

### Le corpus de fixtures, mesuré (pas supposé)

- **`tests/fixtures/manifests/C07/`** — **12 manifestes**, statuts mesurés :
  `ingested=6 · rejected_clouds=3 · rejected_invalid=1 · rejected_nodata=1 · failed=1`.
  Un seul run, `run-2026-01-31`, dont `aggregate_counters` rend **exactement** les chiffres
  de l'oracle O1 : `found_stac=15 · skipped_scene_cloud=1 · off_tile=2 · found_tile=12` +
  les 6 statuts. ✅ **L'oracle O1 est faisable tel quel.**
- ⚠ **Deux pièges dans ce corpus, à connaître avant d'écrire le test d'intégrité** :
  - **tous les `grid_hash` valent `ffffffff…`** (valeur factice) ;
  - **`tile` vaut `31TGM`** partout, alors que `config/sites.yaml` donne `52TEL` à C07.
  Le critère « `grid_hash` == celui de la grille courante » serait donc **ROUGE sur le
  corpus nominal**, ce qui casserait l'oracle O2quater dans son cas « OK ».
- **`tests/fixtures/manifests/OVERLAP/`** — ⭐ **la fixture faite pour `--check-completeness`** :
  **2 runs recouvrants** (`run-A`, `run-B`) dont la somme des compteurs annonce
  `found_tile=10`, alors qu'il n'y a que **7 `item_id` réels**. C'est la démonstration
  vivante de l'arbitrage n°2 : comparer des SOMMES donnerait un écart de 3 items fantômes.
  `item_ids_for_site` rend bien les 7.
- **`tests/fixtures/stac/bi_tile.json`** — 12 items, `reference_tile="52TEL"` :
  **6 `off_tile`** (52TDL) et **2 au-dessus de `scene_cloud_max=95`** (cc 97,9 et 99,3).
  C'est la seule fixture qui porte **les trois pièges** de l'oracle O2ter.

### L'existant à consommer tel quel

- **`adapters/manifests.py`** : `list_for_site` · `list_runs` · `item_ids_for_site` ·
  `aggregate_counters` · `read_manifest` · `grid_hash(grid, settings, …)`.
  ⚠ `aggregate_counters` **sur-compte** dès que des runs se recouvrent (c'est écrit dans sa
  docstring, et OVERLAP le prouve) : c'est une donnée de VOLUME, jamais de complétude.
- **`Manifest`** porte déjà tout ce dont le critère d'intégrité a besoin : `files`,
  `content_hashes`, `grid_hash`, `chip_nodata_pct`, `bytes_written`, `scl_class_counts`.
  ⭐ **Aucune relecture de raster n'est nécessaire** — c'est le point de la fiche.
- **`adapters/contact_sheet.py`** (l0-03.6, mergé) — le point d'extension pour `--first-last` :
  ```python
  class SheetCell: site_id · label_lines · image
  build_contact_sheet(sites, data_root, *, columns=GRID_COLUMNS) -> Image.Image
  write_contact_sheet(sites, data_root, out_path, *, columns=GRID_COLUMNS) -> Path
  latest_ingested_manifest(data_root, site_id) -> Manifest | None
  render_rgb(chip_path) -> Image.Image · stretch_percentile(band, …) -> np.ndarray
  ```
  L'extension était **actée** en l0-03.6 (exception assumée à l'invariant add-only).
- **`core/settings.Settings`** porte `chip_nodata_pct_max` (défaut 1) et `data_root`.
- **`cli/exit_codes.py`** : `OK=0 · FAILURE=1 · USAGE=2 · INCONCLUSIVE=3`.
- **Auto-découverte** : `register(app)` dans `cli/report.py`. ⛔ `__main__.py` figé.
- **Le gate** est `lint && types && test && smoke && cwl` — **208 tests** verts.

### Décisions d'ancrage (prises ici — ne pas rouvrir)

1. **Couches** : `core/report.py` est **PUR** — il prend des `list[Manifest]`, `list[Run]`,
   `Site` et `Settings` **déjà chargés**, et rend le **texte Markdown** (une chaîne).
   `cli/report.py` fait le chargement via l'API manifests, appelle `core/report.py`, écrit
   le fichier. ⛔ `core/` n'importe JAMAIS `adapters/` — mais il peut **typer** ses entrées
   sur `Manifest`/`Run`… ce qui serait un import d'`adapters`. **Résous-le en définissant les
   entrées de `core/report.py` sur des `Protocol`s structurels locaux, ou en passant des
   dicts déjà extraits.** Choisis, documente ton choix en docstring, et ne casse pas la règle
   de couche : c'est la contrainte la plus dure de cette fiche.
2. ⭐ **Le test d'intégrité NE PEUT PAS tourner sur le corpus tel quel** (`grid_hash` factices).
   Copie le corpus dans `tmp_path`, puis **réécris les `grid_hash` avec la vraie valeur**
   calculée par `manifests.grid_hash(grid, settings)` pour la grille du site de test — c'est
   le cas « nominal → `integrite: OK` ». Les trois mutations d'O2quater partent ensuite de ce
   corpus assaini. ⛔ Ne modifie PAS les fixtures versionnées : elles servent déjà des tests
   verts de l0-03.2.
3. **`--check-completeness` se teste sur OVERLAP + une fixture `/search` enregistrée**, sans
   réseau. C'est OVERLAP qui donne l'oracle mordant : `aggregate_counters` y annonce
   `found_tile=10` pour **7 items réels** — un contrôle bâti sur les sommes rendrait un écart
   de 3, un contrôle bâti sur `item_ids_for_site` rend 0. ⚠ La requête témoin doit reproduire
   **les trois filtres** (bbox, tuile de référence, `eo:cloud_cover < scene_cloud_max`) — la
   fiche explique pourquoi l'oubli du troisième rendrait le contrôle rouge en permanence sur
   Hinkley ou Sizewell.
4. **Option réseau, hors gate** : `--check-completeness` fait une vraie requête en production.
   Le test passe par une fixture ; **aucun test ne fait de réseau**.
5. **Ne recopie AUCUN compte en dur depuis la fiche sans l'avoir relu du corpus** : les
   chiffres de l'oracle O1 ci-dessus ont été **mesurés** le 22/08, ils sont exacts — mais
   c'est `aggregate_counters` qui doit les produire dans le test, comparés à des littéraux.
6. **`bytes_downloaded` porte sa mention de portée** dans le rapport (« STAC seul — les
   lectures d'assets par GDAL ne sont pas instrumentables depuis Python », décision E-d).
   Les octets **stockés** viennent de `bytes_written` des manifestes. Le volume réellement
   **transféré** est **hors instrument** : à déclarer non mesuré, jamais à estimer.

## Définition de « terminé »

- [ ] `report` + `--first-last`, tests sur le corpus fixtures de l0-03.2.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | `report` sur le corpus l0-03.2 (**mêmes comptes que l0-03.2/O2**, à ne pas diverger : found_stac=15, skipped_scene_cloud=1, off_tile=2, found_tile=12, puis les 6 statuts ingested=6, rejected_clouds=3, rejected_invalid=1, rejected_nodata=1, failed=1, skipped=0) | rapport EXACT chiffre à chiffre ; `conservation: OK` ; **ratio 6/12 avec dénominateur affiché** ; `failed_pct = 1/12 = 8,3 %` signalé > 1 % ; classes 2/11 agrégées |
| O2 | Corpus muté : `off_tile` porté à 3 (conservation cassée) | le site est marqué **`conservation: ROUGE`** et le rapport le met en tête (prouve que l'oracle sait rendre rouge) |
| O2bis | Corpus muté : `failed` porté à 0 | `failed_pct = 0 %`, aucun ROUGE — le critère à seuil discrimine bien dans les deux sens |
| O2quater | **Intégrité** : corpus nominal → `integrite: OK` ; puis trois mutations séparées — un `ingested` à 2 fichiers · un `grid_hash` périmé · un `chip_nodata_pct` à 5 % | ROUGE à chaque fois, avec **l'`item_id` et la cause** nommés (le critère 3 de la recette sait enfin rougir) |
| O2ter | `report --check-completeness` sur fixture (réponse `/search` enregistrée), corpus à **runs recouvrants** ET contenant **≥ 1 item écarté par le pré-filtre scène** et **≥ 1 item `off_tile`** | écart 0 → OK **malgré le recouvrement, le nuage-scène et le hors-tuile** (les trois pièges dans la même fixture — sinon le test passe vert et le défaut n'apparaît qu'en campagne, au pire endroit : le critère de recette n°2) ; fixture mutée (1 item retiré) → ROUGE avec **l'`item_id` manquant nommé** |
| O3 | `contact-sheet --first-last` sur data_root fixtures | PNG, 2 imagettes/case, comptage exact (test) |
| O4 | `just check` | vert, hors ligne |

**Non testé** : rendu html du rapport (hors périmètre — le .md suffit, conversion
possible via `just md2html` à la main).

---
## Résumé de réalisation
*(à remplir avant `fait/`)*
