---
id: l0-04.H
titre: "[HUMAIN] Campagne historique réelle 25×48 + recette du Lot 0"
effort: H
categorie: humain
phase: O2
depends_on: [l0-04.1, l0-04.2, l0-03.H]
parent: l0-04
---

# [l0-04.H] — ⛔ FICHE HUMAINE — NE PAS DISPATCHER À UN AGENT

> **Réalisée par Philippe** (lancement d'infra + jugement de recette). Le run s'arrête ici
> via `depends_on` — c'est le second et dernier gate humain du Lot 0.

## Objet

Lancer la campagne réelle (machine cible, data_root = NAS), produire le rapport et la
planche, prononcer la recette du Lot 0.

## Checklist Philippe

- [x] `TINY_WAE_DATA_ROOT=<data_root> just run backfill --sites all --months 48 --workers 6`
      **FAIT le 2026-08-23** : 1200/1200 fenêtres, **6 h 13**, 0 retry, 16 Go, 5468 chips
      ingérés, 5 fenêtres en échec (cf. `data-01`). Relançable : idempotent (mesuré, une
      fenêtre déjà faite se rejoue en ~2 s).
      ⚠ **La durée dépend entièrement de `perf-01`** : sans les réglages GDAL, le même run
      était projeté à **~21 h**. Ne pas reprendre l'estimation « 3-5 h » d'origine, qui
      n'avait jamais été mesurée.
- [x] `TINY_WAE_DATA_ROOT=<data_root> just run report --out <data_root>/report.md`
      puis lire ce fichier.
      ⚠ **`--out` est nécessaire** : son défaut est `report.md` **relatif au répertoire
      courant**, donc à la racine du dépôt et non dans le `data_root` (vérifié dans
      `cli/report.py:50`). **Les 4 critères mécaniques sont
      marqués par l'outil, pas par toi** : (1) `conservation: OK` sur les 25 sites et sur
      l'agrégat ; (2) complétude (ci-dessous) ; (3) `integrite: OK` par site (fichiers,
      hashes, `grid_hash` courant, nodata) ; (4) `failed_pct ≤ 1 %` par site.
      *(Numérotation O1→O4 de `docs/lots/lot-0-sites.md` §5.)*
- [x] `TINY_WAE_DATA_ROOT=<data_root> just run report --check-completeness --sites A01,C07,A03 --from 2022-09-01 --to <fin de campagne>`
      → écart **0**. **FAIT le 2026-08-23** : `A01 OK (0 écart)`, `C07 OK (0 écart)`,
      `A03 OK (0 écart)`, exit 0 — après le correctif `data-01` et un backfill de rattrapage
      sur les 5 sites concernés (240 fenêtres en 3 min 57, 28 items valides récupérés).
      ⚠ **`--from` et `--to` sont OBLIGATOIRES** avec `--check-completeness` (sinon exit 2,
      `usage : --check-completeness requiert --sites, --from et --to`). Constaté le
      2026-08-23 : la commande écrite ici sans eux échoue. La requête
      témoin reproduit **les trois filtres du pipeline** (bbox + fenêtre + `grid:code` +
      `eo:cloud_cover < 95 %`) ; tout écart est listé item par item par l'outil.
- [ ] `TINY_WAE_DATA_ROOT=<data_root> just run contact-sheet --first-last --out <data_root>/contact-sheet-first-last.png`
      → contrôle visuel de non-régression.
      ⚠ **`--out` est nécessaire** : son défaut est `contact-sheet.png` **à la racine du
      dépôt** (`cli/contact_sheet.py:30`). C'est le piège déjà rencontré sur `l0-03.H`, où
      il a failli faire relire une planche périmée. La correction n'avait pas été propagée
      jusqu'ici.
- [ ] **Volume transféré** (hors instrument, décision E-d) : relever le compteur réseau
      système avant/après la campagne, OU consigner « non mesuré ». Le rapport donne les
      octets **écrits** (exacts) et les octets STAC (partiels, portée dite) — pas le
      transfert GDAL, non instrumentable depuis Python.
- [ ] **Lecture de jugement** (c'est TA part, non oraclée) : ratios `ingested/found_tile`
      par site avec leurs dénominateurs, `off_tile` et `skipped_scene_cloud`, pires cas en
      tête. Rappel : `off_tile` élevé = site multi-tuiles (C07 ≈ 50 %, A01 = 0 %), ce
      n'est pas un défaut ; un ratio faible sur un site nuageux (Hinkley) non plus.
      Ce qui doit t'alerter : un ratio faible **sans** explication chiffrée dans la
      colonne d'explication automatique.
- [ ] **Recette du Lot 0** : verdict consigné dans `docs/lots/lot-0-sites.md` (§recette) —
      chiffres retenus, réserves, GO/NO-GO pour l'ouverture du Lot 1.
- [ ] Déplacer cette fiche en `fait/` avec le résumé.

## Critère de sortie

Rapport publié ; **les 4 critères mécaniques au vert** (conservation 25/25, complétude
écart 0, `integrite: OK`, `failed_pct ≤ 1 %`) ; recette prononcée dans le document de lot
avec les chiffres retenus et tes réserves sur les ratios (jugement humain assumé).
Les corrections tardives de coordonnées (rares — l0-03.H est passé avant) suivent
la même mécanique `grid_hash` : éditer, re-survey, re-backfill du site, re-report.

---
## Résumé de réalisation
*(rempli par Philippe : chiffres clés, réserves, verdict de recette)*
