---
id: l0-07
titre: "write_manifest refuse un statut illégitime"
effort: S
categorie: durcissement
phase:
depends_on: [l0-03.2]
parent:
subtasks: []
---

# [l0-07] — `write_manifest` refuse un statut illégitime

> Fiche de backlog : sert de **brief (prompt)** pour l'IA.
> Avancement = dossier : `maturation/` → `a-faire/` → `en-cours/` → `fait/`.
> ✅ **Placement validé par le PO le 2026-08-23** (lancement du run) : id `l0-07` hors de la séquence du chantier
> `lot-0-ingestion` (qui s'arrête à `l0-06`), fiche autonome sans `parent:` — sur le
> précédent d'`out-01`. Rattacher au chapeau `l0-03` rouvrirait sa taxonomie, déclarée
> fermée. Le `depends_on: [l0-03.2]` n'est pas décoratif : c'est cette fiche qui a créé
> `adapters/manifests.py` et son patron de garde (`write_run`/`_validate_counters`), que
> celle-ci prolonge. Elle est en `fait/`, la dépendance est donc satisfaite. Le contenu
> ci-dessous ne dépend pas de cet arbitrage.

## Objectif

`Manifest.status` est un `str` libre : rien n'empêche aujourd'hui d'écrire un manifeste
portant `"skipped"`, `"Ingested"` ou une faute de frappe. Or le manifeste est **la source de
vérité relue** par `report`, `contact-sheet` et `update` — un statut inconnu s'y propage
silencieusement, et ne se découvre qu'au moment où un agrégat ne tombe plus juste.

Poser la garde à l'écriture, sur le patron déjà en place pour les compteurs de run.

## Contexte et périmètre

### ⚠ Ancrage dans le code réel (vérifié le 2026-08-22, HEAD `c67bfc1`)

Re-vérifié le 2026-08-23 à HEAD `690e899` avant dispatch : `Manifest.status` toujours
`str` libre, `MANIFEST_STATUSES` toujours absente, et
`tests/test_vocabulary.py::test_manifest_statuses_a_bien_disparu` toujours présent (le
test que cette fiche doit remplacer, pas supprimer). Base de non-régression : **276**
tests depuis `obs-01`/`obs-02`.

- **`Manifest.status: str`** (`adapters/manifests.py:80`) — aucune contrainte, ni au type,
  ni à la construction, ni à l'écriture.
- **Les 5 statuts réellement écrits** viennent tous d'`adapters/ingestion.py` :
  `"failed"` (l. 234, 257, 307), `"rejected_nodata"` (l. 340), `"ingested"` (l. 360) et
  `verdict_result.status` (l. 283), lui-même ∈ `{ingested, rejected_clouds,
  rejected_invalid}` (`core/scl.py:55-59`). Soit **exactement `RUN_STATUSES` moins
  `"skipped"`** — un item déjà ingéré au `grid_hash` courant n'est pas retraité, donc jamais
  écrit.
- **`adapters/update.py:79,93,104` écrit aussi `status=`, mais sur `SiteUpdateResult`, PAS
  sur `Manifest`** — vérifié, hors périmètre. Ne pas s'y tromper à la lecture d'un `grep`.
- **Le patron existe déjà** : `write_run` (`manifests.py:274`) appelle `_validate_counters`
  puis écrit, et lève `ConservationError` — sous-classe de `ManifestError(ValueError)` — **sans
  rien écrire**. La garde de cette fiche doit lui être symétrique.
- **`MANIFEST_STATUSES` a été supprimée** au commit `c67bfc1` : elle existait dans
  `adapters/manifests.py` depuis sa création **sans aucun consommateur** (vérifié sur `src/`,
  `tests/`, `scripts/`, à HEAD comme à `d837c76`). Cette fiche la ré-introduit **dérivée**, et
  cette fois avec un consommateur.
- **Impact nul sur l'existant** : les manifestes de `tests/fixtures/` portent uniquement
  `ingested` (13), `rejected_clouds` (3), `rejected_invalid` (1), `rejected_nodata` (1) et
  `failed` (1) — aucun `skipped`, aucune variante de casse. Les tests ne construisent des
  `Manifest` qu'avec `ingested` et `failed`. **Aucune migration de données n'est requise.**

### ⚠ Ancrage anticipé (posé pendant le dispatch de `data-01`, HEAD `096f8d1`)

`data-01` modifie **le même fichier** que cette fiche (`adapters/manifests.py`) et touche
`core/envelope.py`. L'ancrage ci-dessus se scinde donc :

**Faits INDÉPENDANTS de `data-01`, acquis** :

- `Manifest.status` reste un `str` libre : `data-01` ne touche ni au type ni aux statuts.
- Les 5 statuts réellement écrits par `adapters/ingestion.py` sont inchangés :
  `data-01` écarte des items AVANT parsing, sans jamais écrire de manifeste pour eux.
- Le patron de garde `write_run` -> `_validate_counters` -> `ConservationError` reste le
  modèle à imiter (`data-01` l'étend, ne le remplace pas).
- `RUN_STATUSES` est explicitement hors du périmètre de `data-01` (sa décision D2).

✅ **RECONFIRMÉ après le merge de `data-01`** (HEAD `603be73`, le 2026-08-23) — emplacements
RÉELS, ceux du haut de fiche sont périmés là où c'est indiqué :

- `Manifest.status` : **l. 91** (était 80).
- `write_manifest` : **l. 202**. `write_run` : **l. 291** (était 274).
- `_COUNTER_KEYS` : **l. 51**, forme inchangée (`(*ENVELOPE_COUNTERS, *RUN_STATUSES)`) —
  mais `ENVELOPE_COUNTERS` compte désormais **5** entrées, `skipped_asset_scheme` incluse.
- ⛔ **NOUVEAU, et c'est le piège** : `_READ_TOLERANT_COUNTER_KEYS` (**l. 59**) et
  `_with_read_tolerant_defaults` (**l. 322-326**) introduisent une tolérance à la LECTURE
  des `run.json`. **NE PAS la transposer à la garde de cette fiche** : `write_manifest`
  écrit, elle doit rester STRICTE. La tolérance existe parce qu'un corpus de 1404 journaux
  antérieurs devait rester lisible ; aucun équivalent ici.
- `core/statuses.py` : **intact**, dernier commit `4d89644`, non touché par `data-01`.
- `tests/test_vocabulary.py::test_manifest_statuses_a_bien_disparu` : toujours présent
  (**l. 46**) — le test à REMPLACER, jamais à supprimer.
- **Base de non-régression : 293 tests** (et non 276).

Ancienne liste des points à reconfirmer, pour mémoire :

- **Les numéros de ligne d'`adapters/manifests.py`** cités plus haut (`Manifest.status` l. 80,
  `write_run` l. 274) seront décalés.
- **`_COUNTER_KEYS` (l. 48) change** : `data-01` ajoute `skipped_asset_scheme` à
  `ENVELOPE_COUNTERS`, dont cette constante dérive. Cette fiche n'y touche pas, mais ne doit
  pas se fonder sur son contenu actuel.
- **`_validate_counters` gagne une tolérance à la lecture** (D6 de `data-01`). La garde de
  CETTE fiche porte sur l'ÉCRITURE d'un manifeste : elle reste stricte, sans tolérance.
  Ne pas transposer par mimétisme la souplesse introduite à côté.
- **La base de non-régression ne sera plus 276 tests** mais celle laissée par `perf-01` et
  `data-01`. À relever au démarrage, pas à supposer.

### Fichiers touchés

- `src/tiny_wae/core/statuses.py` — ré-introduire `MANIFEST_STATUSES`, **dérivée** de
  `RUN_STATUSES`, jamais re-listée. Mettre à jour le docstring du module, qui explique
  aujourd'hui pourquoi elle a été supprimée.
- `src/tiny_wae/adapters/manifests.py` — `ManifestStatusError(ManifestError)` + garde dans
  `write_manifest`.
- `tests/test_manifests.py` — les oracles ci-dessous.
- `tests/test_vocabulary.py` — `test_manifest_statuses_a_bien_disparu` devient faux : il
  asserte l'absence de la constante. **Le remplacer**, ne pas le supprimer : il doit
  désormais vérifier que la constante est dérivée et qu'elle a bien un consommateur.

### ⭐ Décision actée : la garde est à l'ÉCRITURE, pas à la construction

Pas de validation dans `Manifest.__post_init__`. Deux raisons : la symétrie avec `write_run`
(c'est l'écriture qui fige une vérité sur disque, un objet en mémoire ne fige rien), et le
fait qu'un test de chemin d'erreur doit pouvoir construire un `Manifest` invalide pour le
présenter à la garde. Ne pas « améliorer » en durcissant le dataclass.

## Définition de « terminé »

- [ ] `MANIFEST_STATUSES: frozenset[str]` dans `core/statuses.py`, **dérivée** :
      `frozenset(RUN_STATUSES) - {"skipped"}`. Aucune liste écrite à la main.
- [ ] `ManifestStatusError(ManifestError)` définie dans `adapters/manifests.py`, à côté de
      `ConservationError`.
- [ ] `write_manifest` valide `manifest.status` **avant** toute écriture et lève sans rien
      créer sur disque — ni le `manifest.json`, ni un `.tmp` résiduel.
- [ ] Le message d'erreur nomme **le statut refusé** et **la liste des statuts admis**.
- [ ] `read_manifest` **n'est pas** durci (cf. non-testé).
- [ ] `tests/test_vocabulary.py` mis à jour, pas supprimé.

## Oracle / recette (figé AVANT implémentation)

| # | Critère mesuré | Seuil de succès |
|---|---|---|
| O1 | `write_manifest` sur un `Manifest` à `status="skipped"` | lève `ManifestStatusError` **et** le répertoire cible reste vide : ni `manifest.json`, ni fichier `.tmp` |
| O2 | idem avec `status="Ingested"` (casse) puis `status="ingere"` (faute) | lève dans **les deux** cas — la garde n'est pas une simple exclusion de `"skipped"` |
| O3 | les 5 statuts légitimes, un par un | **5/5** écrivent normalement et se relisent identiques par `read_manifest` |
| O4 | contenu du message de `ManifestStatusError` | contient **littéralement** le statut refusé, et les 5 statuts admis |
| O5 | non-régression | `just check` vert — **276 tests** au départ, aucun test existant modifié hors `test_vocabulary.py` |

**Non testé par cette fiche** (chiffres honnêtes) :

- **Pas de test de mutation** (règle projet posée par le PO le 2026-08-22 : « on fait des
  tests, ils doivent passer au vert »). L'oracle de mutation qui figurait ici a été retiré.
  Écrire O1 **avant** la garde, et constater qu'il est rouge, suffit — c'est gratuit et ça
  se fait dans le fil de l'implémentation.
- **`read_manifest` reste permissif, délibérément.** Durcir la lecture rendrait illisible un
  corpus historique écrit par une version antérieure — on refuserait de relire des données
  qu'on a nous-mêmes produites. La garde protège ce qu'on écrit, pas ce qu'on a écrit.
- **Aucun autre champ de `Manifest` n'est validé** (`tile`, `sequence`, `platform`,
  `schema_version`…) : même classe de défaut, hors périmètre, à instruire séparément si le
  besoin se confirme.
- **`Run.status`-like** : les compteurs de `run.json` sont déjà couverts par
  `_validate_counters`, rien à ajouter de ce côté.
- **Aucune migration** : mesuré, aucun manifeste existant ne porte de statut hors des 5.
- **Pas de campagne réelle** : la fiche ne demande aucun run réseau.

## Notes / pistes

Origine : post-revue 1 du Lot 0, en marge du constat A2 — la correction `c67bfc1` a montré
que `MANIFEST_STATUSES` ne servait à rien, ce qui revient à dire que **la règle qu'elle
portait n'était appliquée nulle part**. Supprimer la constante était juste ; laisser la règle
non appliquée ne l'est pas.

Coût attendu : une dizaine de lignes de code, l'essentiel de l'effort étant dans les tests.

---

## Résumé de réalisation

*(à remplir avant de déplacer la fiche dans `fait/`)*

- **Ce qui a été fait** : …
- **Verdict de l'oracle** : [chiffres obtenus, y compris défavorables]
- **Commit(s)** : …
- **Date** : AAAA-MM-JJ
