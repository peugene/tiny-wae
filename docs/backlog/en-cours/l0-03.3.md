---
id: l0-03.3
titre: Lecture COG fenêtrée + écriture chip.tif / chip_20m.tif / scl.tif
effort: M
categorie: pipeline
phase: O1
depends_on: [l0-03.1, l0-02.1]
parent: l0-03
---

# [l0-03.3] — Chips : lecture et écriture

## Objectif

Depuis une `Acquisition` : lire les fenêtres nécessaires (SCL d'abord), écrire les 3
fichiers sur la grille du site. Pas de CLI ici (l0-03.4), pas de manifeste (fonction pure
retournant un résultat que l0-03.4 assemble).

## Périmètre (fichiers nommés)

- `src/tiny_wae/adapters/chips.py` :
  - `read_scl(acq, grid) -> ndarray 256×256` — fenêtre dérivée **ici** de
    `core.geometry.chip_bounds` (l0-03.1) via `rasterio.windows.from_bounds` sur le
    transform de la source ; `Resampling.nearest`
    si jamais nécessaire — normalement AUCUN rééchantillonnage, fenêtre entière.
  - `read_bands_10m(acq, grid)`, `read_bands_20m(acq, grid)` — 4 et 6 bandes (D-b :
    rededge1/2/3 + nir08 + swir16 + swir22), dans CET ordre, gravé en constante
    `BAND_ORDER_10M` / `BAND_ORDER_20M` (l'ordre des bandes est un contrat du Lot 1).
  - `write_chips(dest_dir, arrays, grid)` : **GeoTIFF simple, PAS COG (G5)**, uint16,
    transform de `core.geometry.transform_for`, tags (item_id, band order).
  - Garde : `acq.proj_epsg == grid.epsg` sinon exception typée (→ `failed`/`tile_mismatch`
    dans l0-03.4). ⭐ **Compteur `assets_read`** exposé (décision E-d) : nombre
    d'ouvertures de raster effectuées par ce module — l'instrument d'idempotence du lot.
    Pas de comptage d'octets ici (les lectures passent par GDAL, non instrumentable).
  - ⭐ **Garde réseau de contrat (décision E-b, 21/08)** : sous `TINY_WAE_OFFLINE=1`,
    **refuser tout href dont le schéma n'est pas `file://`** → exception typée
    `RemoteAccessForbidden`, levée **AVANT toute ouverture** (aucune requête émise).
    C'est ICI qu'elle vit — `chips.py` est le seul point qui ouvre les rasters ; la
    placer dans `FixtureSource` seul serait contournable. ⚠ Motif mesuré en revue v3 :
    `pytest-socket` ne couvre PAS les lectures GDAL (C/libcurl), donc une garde de
    transport laisserait passer un smoke « vert en téléchargeant ».
- Dépendance utilisée : `rasterio` — **déjà posée par l0-01.1** (ne pas toucher au
  `pyproject.toml`). `pixi.lock` : versionné, jamais mergé — régénérer au besoin.
- Tests : sur les GeoTIFF fixtures locaux de l0-03.5 s'ils existent déjà, SINON sur des
  rasters synthétiques écrits par le test (les deux marchent — ne pas attendre l0-03.5).

## ⚠ Ancrage dans le code réel (22/08/2026, avant dispatch — run N3)

Vérifié sur `develop` @ `55ab4ec` (l0-03.1, l0-03.2 et l0-02.1 mergées) — **faits, pas
suppositions** :

- **`core/geometry.py` (l0-03.1) est en place** : `chip_bounds(grid, settings) ->
  (minx, miny, maxx, maxy)` dans le CRS du site (coin haut-gauche = `origin_x`/`origin_y`,
  emprise 10 m et 20 m identiques par construction) ; `transform_for(grid, resolution) ->
  affine.Affine` (résolution en mètres : `10` ou `20`). Exceptions typées déjà là :
  `GridNotComputedError`, `InconsistentChipSizesError`. ⛔ **Ne rien recalculer à la main** :
  la fenêtre rasterio se dérive de `chip_bounds` via
  `rasterio.windows.from_bounds(*bounds, transform=src.transform)`.
- **`core/bands.py` (l0-03.1) porte DÉJÀ les deux ordres** : `BAND_ORDER_10M =
  ("blue","green","red","nir")` (4) et `BAND_ORDER_20M =
  ("rededge1","rededge2","rededge3","nir08","swir16","swir22")` (6). ⛔ **Les importer, ne
  jamais les redéfinir** (la fiche dit « gravé en constante » — c'est fait).
- **`core/acquisition.Acquisition`** : `proj_epsg: int` (déjà normalisé — l'adapter accepte
  `proj:epsg` OU `proj:code`, cf. chapeau l0-02), `assets: dict[str, str]` (clé d'asset →
  href), `radiometry: dict[str, tuple[float,float] | None]`, `item_id`, `datetime`, `tile`.
  ⭐ **Passer par ce contrat, jamais par une clé STAC brute.**
- **`core/sites.Grid`** : `epsg: int | None`, `origin_x: float | None`, `origin_y: float | None`.
  Les 25 grilles sont **REMPLIES** dans `config/sites.yaml` depuis l0-01.3 (ex. A01 :
  epsg 32631, origin 721080 / 4845740).
- **Le manifeste aval attend des noms précis** (`adapters/manifests.Manifest`, l0-03.2, déjà
  écrit) : `assets_read: int` · `content_hashes: dict[str, str]` · `bytes_written: int` ·
  `chip_nodata_pct: float` · `files: list[str]`. ⭐ **C'est le contrat de sortie de cette
  fiche** : reprendre ces noms tels quels pour que l0-03.4 recopie sans traduire.
- **`core/scl.verdict(scl_class_counts: dict[int,int], settings) -> Verdict`** existe déjà
  (l0-03.2) et consomme un **comptage**, pas un tableau.
- **Versions mesurées** : rasterio **1.5.1** · affine 3.0.0 · numpy 2.5.2. ⛔ Aucune montée
  de version, et **`pyproject.toml` n'est PAS touché** (rasterio y est déjà, en dépendance
  conda-forge) — une autre fiche y écrit en parallèle sur ce run.

### Décisions d'ancrage (prises ici — ne pas rouvrir, ne pas re-délibérer)

1. **Forme du retour (contrat vers l0-03.4)** : `adapters/chips.py` expose un dataclass
   `frozen=True, slots=True` nommé `ChipResult`, portant au moins `files: list[str]`,
   `assets_read: int`, `content_hashes: dict[str, str]`, `bytes_written: int`,
   `chip_nodata_pct: float` — noms alignés sur `Manifest` (voir ci-dessus).
2. **`assets_read` remonte PAR VALEUR DE RETOUR**, jamais par un compteur de module :
   ⛔ **aucun global mutable** — l0-04.1 fera tourner plusieurs ingestions dans un pool de
   workers, un compteur de module y serait faux **silencieusement**. Chaque `read_*` renvoie
   son nombre d'ouvertures avec son tableau ; l0-03.4 les somme.
3. **`scl_class_counts` n'est PAS du périmètre de cette fiche** : `read_scl` renvoie le
   ndarray ; le comptage des classes et l'appel à `core.scl.verdict` restent à l0-03.4.
4. **Unité de `chip_nodata_pct` : un POURCENTAGE (0–100)**, pas une fraction 0–1 — même
   unité que `settings.chip_nodata_pct_max` (défaut `1`) et que l'oracle O3 (« 0,5 % →
   ingested ; 40 % → rejected_nodata »). Valeur nodata = `0` (convention S2 L2A) ; la
   fraction se mesure sur le chip **10 m**.
5. **Garde réseau `TINY_WAE_OFFLINE`** : nom EXACT de la variable d'environnement (aucune
   variante), active quand elle vaut `"1"`. `RemoteAccessForbidden` est levée **avant toute
   ouverture** (aucune requête émise, pas même une sonde). ⚠ **Schémas acceptés** : `file://`
   **et un chemin nu sans schéma** (`/…/x.tif` — c'est la forme que prendront les fixtures
   locales de l0-03.5, et rasterio les ouvre telles quelles). Refusés : `http://`, `https://`,
   `s3://`, et tout autre schéma. Écrire ce choix en docstring : la fiche dit « schéma
   != file:// », le chemin nu en est l'extension nécessaire.
6. **Écriture** : GeoTIFF **simple, PAS COG** (G5) ; `uint16` pour les bandes, `uint8` pour le
   SCL ; `transform_for(grid, 10)` / `transform_for(grid, 20)` ; CRS = `EPSG:{grid.epsg}` ;
   tags `item_id` + ordre des bandes. Noms de fichiers figés : `chip.tif`, `chip_20m.tif`,
   `scl.tif`.
7. **Hash de contenu (O1bis)** : `sha256` sur `array.tobytes()` **+** `f"EPSG:{grid.epsg}"`
   **+** les 6 coefficients du `transform` formatés **+** `array.dtype.str`. ⛔ **Jamais** les
   octets du fichier GeoTIFF (le WKT PROJ embarqué dérive entre versions et plateformes —
   l'oracle deviendrait un détecteur de version de GDAL).
8. **Tests sur rasters synthétiques** (l0-03.5 n'existe pas encore) : le test écrit lui-même
   ses GeoTIFF sources avec rasterio, sur une grille et un EPSG connus, avec des valeurs de
   pixels choisies — c'est ce qui rend O1 discriminant (valeur attendue au pixel près, pas
   seulement une forme de tableau).

## Définition de « terminé »

- [ ] Lecture fenêtrée + écriture des 3 fichiers, ordres de bandes gravés, garde epsg.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | Raster synthétique connu → chips | chip.tif 512×512×4 uint16, chip_20m.tif 256×256×6, scl.tif 256×256 uint8 ; CRS/transform == grille au bit près ; valeurs pixel == attendues ; **`assets_read` == nombre d'ouvertures attendu (> 0)** |
| O1bis | **Hash de contenu** (E-d) : `sha256(array.tobytes())` + CRS + transform + dtype des 3 sorties | stable entre deux exécutions consécutives ; **jamais** de hash des octets du GeoTIFF (WKT PROJ versionné → dérive plateforme) |
| O2 | Acquisition epsg ≠ grille | exception typée, rien d'écrit |
| O2bis | Href `https://` sous `TINY_WAE_OFFLINE=1` | `RemoteAccessForbidden` levée avant ouverture ; sans la variable, l'ouverture est tentée normalement |
| O3 | Fraction nodata du chip calculée et retournée (arbitrage n°3) | == fraction injectée dans le raster synthétique ; la valeur **remonte** dans `chip_nodata_pct` du manifeste (l0-03.2) et déclenche `rejected_nodata` au-delà de `chip_nodata_pct_max` — testé dans les deux sens (0,5 % → ingested ; 40 % → rejected_nodata, fichiers supprimés) |
| O4 | `just check` | vert, hors ligne |

**Non testé** : lecture HTTPS réelle (smoke `--live`, l0-03.7) ; performance et volumes
(mesurés en **l0-03.7 / O5**).

---
## Résumé de réalisation
*(à remplir avant `fait/`)*
