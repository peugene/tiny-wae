---
id: l0-04.2
titre: CLI report (agrégats, ratios, SCL) + contact-sheet --first-last — testés sur fixtures
effort: M
categorie: pipeline
phase: O2
depends_on: [l0-03.6]   # étend contact-sheet ; manifests/fixtures par transitivité
parent: l0-04
---

# [l0-04.2] — Report et planche first/last

## Objectif

L'instrument de la recette — donc TESTÉ sur des comptes connus (la recette ne peut pas
être auto-certifiée par un composant non vérifié). Livre aussi la **commande de contrôle
de complétude** que la fiche humaine l0-04.H exécute (critère 2 de l'oracle du chapeau).

## Périmètre (fichiers nommés)

- `src/tiny_wae/core/report.py` (agrégation PURE sur les données de l'API manifests) +
  `src/tiny_wae/cli/report.py` : `report [--out <path>]` (défaut `<data_root>/report.md`).
  Par site, **dans cet ordre** (décision E-a) :
  - **Colonne de conservation** : `found_stac`, `skipped_scene_cloud`, `off_tile`,
    `found_tile`, puis `ingested`, `rejected_clouds`, `rejected_invalid`, `failed`,
    `skipped` — avec une **colonne de verdict `conservation: OK/ROUGE`** par site (les
    deux invariants du chapeau l0-02).
  - **`failed_pct` = failed / found_tile** (le seul critère à seuil : ≤ 1 %).
  - **Publiés sans seuil** : `off_tile`, `skipped_scene_cloud`, et le **ratio
    `ingested / found_tile` avec son dénominateur affiché**. Section « pires cas en
    tête » triée sur ce ratio, avec une phrase d'explication automatique factuelle
    (« multi-tuiles : off_tile=317/636 » ou « nuages : rejected_clouds=…/… »).
  - Agrégat `scl_class_counts` (classes 2 et 11 mises en avant — instrument V3),
    `assets_read` agrégé, période couverte, et **volumes** : `bytes_downloaded` **avec sa
    mention de portée** (« STAC seul — les lectures d'assets par GDAL ne sont pas
    instrumentables depuis Python », décision E-d) + octets **stockés** (somme des tailles
    de fichiers listés au manifeste — champ `files` + `bytes_written` à porter par
    l0-03.2). Le volume réellement **transféré** en campagne est **hors instrument** : à
    mesurer au niveau système par l'humain (l0-04.H), ou à déclarer non mesuré.
  Aucune « cohérence statistique » (supprimée en E-a : non calculable — `cloud_pct`
  n'existe pas pour les items non instruits — et circulaire là où elle l'est).
  Sortie Markdown, logs STDERR.
- `cli/contact_sheet.py` : ajout du mode `--first-last` (premier et dernier chip
  `ingested` par site, 2 imagettes par case) — extension actée en l0-03.6.
- **`report --check-completeness --sites A01,C07,…`** (critère 2 de l'oracle du chapeau) :
  pour chaque site demandé, rejoue une requête `/aggregate` **aux mêmes bbox et fenêtre,
  SANS filtre tuile** et compare à `found_stac` agrégé — **tolérance 0**, tout écart
  listé item par item. ⚠ Le comparant est `found_stac`, jamais `found_tile` (l'écart
  serait alors de ~50 % sur les sites multi-tuiles — mesuré sur C07 : 636 vs 319).
  Option réseau, hors `just check`.

## Définition de « terminé »

- [ ] `report` + `--first-last`, tests sur le corpus fixtures de l0-03.2.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | `report` sur le corpus l0-03.2 (comptes connus : found_stac=15, skipped_scene_cloud=1, off_tile=2, found_tile=12, ingested=7, rejected_clouds=3, rejected_invalid=1, failed=1, skipped=0) | rapport EXACT chiffre à chiffre ; `conservation: OK` ; **ratio 7/12 avec dénominateur affiché** ; `failed_pct = 1/12 = 8,3 %` signalé > 1 % ; classes 2/11 agrégées |
| O2 | Corpus muté : `off_tile` porté à 3 (conservation cassée) | le site est marqué **`conservation: ROUGE`** et le rapport le met en tête (prouve que l'oracle sait rendre rouge) |
| O2bis | Corpus muté : `failed` porté à 0 | `failed_pct = 0 %`, aucun ROUGE — le critère à seuil discrimine bien dans les deux sens |
| O2ter | `report --check-completeness` sur fixture (réponse `/aggregate` enregistrée) | écart 0 → OK ; fixture mutée (1 item retiré) → ROUGE avec l'item listé |
| O3 | `contact-sheet --first-last` sur data_root fixtures | PNG, 2 imagettes/case, comptage exact (test) |
| O4 | `just check` | vert, hors ligne |

**Non testé** : rendu html du rapport (hors périmètre — le .md suffit, conversion
possible via `just md2html` à la main).

---
## Résumé de réalisation
*(à remplir avant `fait/`)*
