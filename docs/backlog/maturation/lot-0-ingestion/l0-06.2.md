---
id: l0-06.2
titre: update.cwl — le tool du run quotidien
effort: S
categorie: outillage
phase: O3
depends_on: [l0-06.1, l0-05.2]
parent: l0-06
---

# [l0-06.2] — Tool CWL update

## Périmètre (fichiers nommés)

- `cwl/update.cwl` (CommandLineTool v1.2, `baseCommand: [python, -m, tiny_wae, update]`),
  entrées : liste de sites optionnelle, `TINY_WAE_DATA_ROOT` via EnvVarRequirement ;
  sortie : le résumé (exit code + STDERR — pas de fichier de sortie CWL obligatoire au
  POC, le dire dans le README).
- `cwl/README.md` complété (section update + note : l'ordonnancement du tool en cron/
  PID-FLOW est hors lot).

## Définition de « terminé »

- [ ] `update.cwl` validé, README complété.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | `just cwl` | 0 erreur (4 fichiers désormais) ; `just check` vert |
| O2 [local, hors gate] | `cwltool update.cwl` sur data_root issu du run O2 de l0-06.1 | exit 0, `ingested == 0` (rien de neuf sur fenêtre close) — comportement identique au CLI |

**Non testé** : exécution planifiée réelle (infra).

---
## Résumé de réalisation
*(à remplir avant `fait/`)*
