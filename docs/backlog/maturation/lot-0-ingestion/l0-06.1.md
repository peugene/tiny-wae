---
id: l0-06.1
titre: search.cwl + ingest.cwl + workflow.cwl + README + recette just cwl dans le gate
effort: S
categorie: outillage
phase: O3
depends_on: [l0-02.2, l0-03.4]   # emballe le CLI `search` (l0-02.2) ET `ingest` (l0-03.4)
parent: l0-06
---

# [l0-06.1] — Tools CWL search/ingest + workflow

## Périmètre (fichiers nommés)

- `cwl/search.cwl`, `cwl/ingest.cwl` (CommandLineTool, **CWL v1.2 en 1re ligne**,
  `baseCommand: [python, -m, tiny_wae, <cmd>]`) ; `cwl/workflow.cwl` : step search →
  fichier enveloppe JSON → step ingest `--acquisitions` (le chaînage repose sur
  l'invariant STDOUT-JSON de l0-02.2).
- `cwl/README.md` : conventions, écart assumé vs cwl-assets (package installé, pas de
  scripts rsyncés), périmètre (PAS d'enregistrement server, PAS de pid:Milestone — cf.
  chapeau), commandes de validation et de run local.
- `justfile` : recette `cwl` (`cwltool --validate` sur tous les `.cwl`) ajoutée à
  `check`. Dépendance utilisée : `cwltool` — **déjà posée par l0-01.1** (ne pas toucher au
  `pyproject.toml`). ⚠ Conséquence assumée : à partir de cette fiche, `just check` exige
  `cwltool` dans tous les worktrees. `pixi.lock` : versionné, jamais mergé.

## Définition de « terminé »

- [ ] 3 fichiers CWL validés + README + gate branché.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | `just cwl` | 0 erreur sur les 3 fichiers ; `just check` vert avec la recette intégrée |
| O2 [local, hors gate] | `cwltool workflow.cwl` sur 1 site / 10 jours, `TINY_WAE_DATA_ROOT` VIERGE, fenêtre PASSÉE ET CLOSE (`--to` ≤ J−7) vs run CLI direct sur un second data_root vierge | ensembles `(item_id, statut)` identiques ; tout `failed` invalide la comparaison (rejouer) |
| O3 | STDOUT du step search | JSON parsable seul (test) |

**Non testé** : tool update (l0-06.2) ; worker PID-FLOW (hors lot).

---
## Résumé de réalisation
*(à remplir avant `fait/`)*
