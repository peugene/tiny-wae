---
id: l1-05.4
titre: Trajectoire témoin — Hinkley Point C contre Bouconne, le signal existe-t-il ?
effort: M
categorie: pipeline
phase: O3
depends_on: [l1-05.3]
parent: l1-05
subtasks: []
---

# [l1-05.4] — La question que le POC doit trancher

> Fiche de backlog : sert de **brief (prompt)** pour l'IA.

## Objectif

⭐ **Répondre par la mesure à la seule question que l'expertise ne peut pas trancher : un
vecteur unique couvrant 26 km² porte-t-il un changement local ?**

Une nouvelle halle de 200 m sur un chantier, c'est **0,15 % de la scène**. Si le signal se
noie, le Lot 2 mesurerait de la dérive saisonnière et rien d'autre — et on ne le découvrirait
qu'après l'avoir construit.

## Contexte et périmètre

`src/tiny_wae/cli/trajectory_report.py` → `data/trajectory-report.md`.

⭐ **Le dispositif existe déjà dans le corpus, et il a été choisi pour ça.**

| Site | Ce qu'il est | Ce qu'on en attend |
|---|---|---|
| **A02** Hinkley Point C | chantier EPR **massif**, terrassement puis génie civil, sur les 48 mois | la trajectoire doit **s'éloigner** de son point de départ, franchement et durablement |
| **C08** Bouconne | forêt, **zone témoin** retenue lors de l'arbitrage G10 | la trajectoire doit **osciller** autour de son origine — saisons, rien d'autre |

Ce que la fiche calcule, avec `trajectory_drift` de `l1-05.1`, **pour chaque modèle** :

1. la **dérive** de chaque site dans le temps, référencée au premier vecteur ;
2. ⭐ le **rapport de dérive** A02 / C08 en fin de période — le chiffre qui décide ;
3. la **corrélation dérive ↔ `cloud_pct`** sur C08. ⚠ **C'est le contrôle négatif du choix
   de ne pas masquer les nuages** : si la trajectoire du site stable suit la couverture
   nuageuse, alors les embeddings encodent la météo et le seuil de 10 % ne suffit pas ;
4. la même chose pour **tous les sites**, en annexe — A02 et C08 sont les deux témoins, mais
   la distribution complète situe leur cas.

**Hors périmètre** : toute détection opérationnelle, tout seuil d'alerte, toute datation
d'événement (Lot 2) · la variante 4 tuiles, dont seul le **coût** est chiffré, par `l1-04.3`.

## Définition de « terminé »

- [ ] `cli/trajectory_report.py` livré, `data/trajectory-report.md` produit.
- [ ] Dérives de A02 et C08 tracées dans le temps, **pour les deux modèles**.
- [ ] Rapport de dérive publié.
- [ ] Corrélation dérive ↔ `cloud_pct` sur C08 publiée.
- [ ] ⭐ **Un verdict écrit en toutes lettres** sur la viabilité de l'unité « un vecteur par
      chip », avec ses réserves.
- [ ] `tests/test_trajectory_report.py` — court, sur vecteurs de fixture.

## Oracle / recette (figé AVANT implémentation)

| # | Critère mesuré | Seuil de succès |
|---|---|---|
| O1 | ⭐ **Rapport de dérive** A02 / C08 en fin de période, par modèle | **> 2** sur au moins un des deux modèles pour que l'unité soit déclarée **viable**. Entre 1 et 2 : **douteuse**, la fiche le dit. ≤ 1 : **non viable**, et c'est un résultat majeur qu'il faut annoncer sans le maquiller |
| O2 | **Témoin négatif** : la dérive de C08 contre celle de **deux autres sites stables** du corpus (à choisir parmi ceux sans chantier connu) | C08 n'est pas un cas particulier — sa dérive est du même ordre. Sinon, le témoin est mal choisi et la conclusion de O1 ne tient pas |
| O3 | ⭐ **Corrélation dérive ↔ `cloud_pct` sur C08** | coefficient **publié**. ⚠ Si **|r| > 0,5**, le rapport doit conclure que **le seuil de 10 % est insuffisant** et le remonter à `l1-06.H` comme point d'arbitrage |
| O4 | Monotonie de la dérive de A02 | tendance croissante sur la période, **testée statistiquement** (une corrélation avec le temps suffit — pas besoin de plus pour un POC) |
| O5 | Annexe : dérive de tous les sites | présente ; les 3 sites de plus forte et de plus faible dérive **nommés** |
| O6 | `just check` | vert |

**Non testé par cette fiche** : la **datation** du changement (quand exactement la halle
est-elle apparue) — Lot 2 · la capacité à distinguer deux types de changement · le
comportement de l'unité 4 tuiles, non produite.

## Notes / pistes

⚠ **Le piège de la lecture.** Une dérive forte sur A02 **ne prouve pas** que le modèle voit
le chantier : elle prouve que quelque chose change. Le contrôle, c'est C08 — et O2 vérifie
que C08 lui-même n'est pas atypique. Sans ces deux garde-fous, on lirait dans les courbes ce
qu'on espère y voir.

⚠ **Cette fiche a le droit de rendre un verdict négatif**, et c'est même sa valeur
principale. Un « l'unité par chip ne porte pas le signal » livré ici coûte quelques heures de
CPU ; le même constat au milieu du Lot 2 coûterait le Lot 2.

⚠ **Politique de test** : le rapport se teste sur des vecteurs de fixture ; la campagne
elle-même ne se teste pas, elle se mesure et se publie.

Piste de lecture : tracer les courbes en PNG à côté du rapport aide énormément à l'arbitrage
de `l1-06.H` — `matplotlib` n'est pas au contrat, mais `pillow` l'est déjà et le
`contact_sheet` du Lot 0 montre qu'un rendu simple suffit. **Arbitrer avant d'ajouter une
dépendance.**

---

## Résumé de réalisation

*(à remplir avant de déplacer la fiche dans `fait/`)*

- **Rapport de dérive A02 / C08** : *(par modèle)*
- **Corrélation dérive ↔ cloud_pct sur C08** : …
- **Verdict sur l'unité « un vecteur par chip »** : …
- **Commit(s)** : …
- **Date** : AAAA-MM-JJ
