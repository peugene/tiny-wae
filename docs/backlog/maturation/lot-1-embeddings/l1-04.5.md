---
id: l1-04.5
titre: Rapport de banc — comparatif, extrapolations, ratio discrimination/seconde
effort: M
categorie: outillage
phase: O3
depends_on: [l1-04.2, l1-04.3a, l1-04.3b, l1-04.4, l1-05.3.H]
parent: l1-04
subtasks: []
---

# [l1-04.5] — Le rapport qui rend le banc décidable

> Fiche de backlog : sert de **brief (prompt)** pour l'IA.
> **Révisée post-revue v1** (K-06, K-07, E-31) : le rapport vient désormais APRÈS la
> campagne — c'est lui qui porte le **ratio discrimination / seconde** (critère de sortie
> n°5 du lot, qui n'avait aucun porteur) et la confrontation durée réelle ↔ extrapolation.
> Destination versionnée : `docs/lots/lot-1/`, plus jamais `data/` (gitignoré — les
> rapports seraient morts avec le worktree).
> **Révisée post-revue v3** (S-4/E-7, V-13, V-15) : le rapport agrège des **JSON** — or deux
> de ses sources ne produisaient que de la prose ; et « les vecteurs présents » ne
> désignait aucune spec en particulier.
> ⭐ **Zone d'écriture** : `docs/lots/lot-1/` est **ouverte à l'équipe en création** par
> décision Philippe du 25/08 (revue v3, V-13) — cette fiche y **dépose** son rapport. Le
> reste de `docs/lots/` (les fiches de lot elles-mêmes) demeure zone PO.

## Objectif

Rassembler toutes les mesures en **un document sur lequel Philippe arbitre** en `l1-06.H`.
Il agrège les JSON — il ne mesure rien lui-même, et se régénère à l'identique.

⭐ **Ses sources sont TOUTES machine-lisibles** (revue v3, S-4/E-7). Deux ne l'étaient pas :
`l1-05.3a` ne produisait qu'un `.md` (il produit désormais aussi un `.json`) et la campagne
`l1-05.3.H` ne laissait que de la prose (elle produit désormais `campaign.json` : durée
réelle, périmètre, réglages). Sans ces deux fichiers, ni le **ratio
discrimination/seconde** (point 5) ni la **confrontation à la durée réelle** (point 4) ne
peuvent être calculés autrement qu'en recopiant des nombres à la main — ce qui n'est pas
régénérable.

⚠ **Sélection des vecteurs** : ce rapport lit les vecteurs par
`l1-03.1::list_vectors(model_id=…, spec_hash=…)`, **jamais** « ceux qui traînent dans
`data_root` » — la coexistence de deux specs est un scénario **exigé** par `l1-03.2`/O5.
La spec agrégée est **nommée dans le rapport**.

## Contexte et périmètre

`src/tiny_wae/cli/bench_report.py` → **`docs/lots/lot-1/bench-report.md`**.

Contenu, dans cet ordre :

1. **Contexte machine** — cœurs, RAM, torch, emplacement du corpus, et les **compteurs de
   paramètres des deux modèles côte à côte** (~302 M / ~21 M) : la réserve d'asymétrie du
   chapeau `l1-02` se lit ici.
2. **Comparatif** : temps par phase (médiane, p90), RAM, dim, tokens — par modèle.
3. ⭐ **Extrapolations, sur les DEUX dénominateurs** (revue E-31) : corpus complet (5 793)
   ET corpus filtré (~4 959 au seuil par défaut) — et en deux temps :
   - **le run quotidien** (cadence du Lot 0) — le chiffre qui décide si le système vit ;
   - **la reprise complète** — coût unique, étalable.
   Chaque extrapolation porte son dénominateur et la mention « linéaire, donc optimiste ».
4. ⭐ **Confrontation** : durée réelle de la campagne, **lue dans `campaign.json`**
   (`l1-05.3.H`), contre l'extrapolation — **écart publié en %**. C'est la mesure qui juge
   l'extrapolation, pas l'inverse.
5. ⭐ **Le ratio discrimination / seconde**, par modèle : marge de séparabilité **lue dans
   `similarity-report.json`** (`l1-05.3a`) rapportée au temps médian par chip. **C'est le
   critère de choix du modèle** — numérateur et dénominateur enfin au même endroit, et tous
   deux lus d'un fichier, jamais recopiés.
6. **Les leviers**, chacun avec son facteur et sa limite — y compris les non concluants.
7. **Ce qui n'a pas été mesuré.**

**Hors périmètre** : toute mesure nouvelle · l'arbitrage (`l1-06.H`) · toute
recommandation d'achat.

## Définition de « terminé »

- [ ] `docs/lots/lot-1/bench-report.md` produit, versionné, régénérable sans re-mesure,
      **plus sa vue `.html`** (`just md2html`) — les trois rapports décisionnels sont ce que
      Philippe lit en `l1-06.H`, dans un projet où tout le reste a sa page rendue.
- [ ] Ratio discrimination/seconde présent, par modèle.
- [ ] Les deux extrapolations, la confrontation, la section « non mesuré ».
- [ ] `tests/test_bench_report.py` — sur JSON de fixture.

## Oracle / recette (figé AVANT implémentation)

| # | Critère mesuré | Seuil de succès |
|---|---|---|
| O1 | Rapport depuis des JSON de fixture aux valeurs connues | chiffres exacts, extrapolations et ratio **recalculables à la main** |
| O2 | ⭐ Un JSON de levier `status: "inconclusive"` | apparaît comme tel — **jamais omis**. Un rapport qui ne montre que ce qui a marché ment |
| O3 | Un JSON manquant — y compris `campaign.json` ou `similarity-report.json` | signalé explicitement, pas un tableau à trou ; et le ratio discrimination/seconde est **absent avec sa raison**, jamais calculé sur une valeur devinée |
| O3b | Deux specs présentes dans `data_root` | le rapport **nomme** celle qu'il a agrégée, et n'en agrège **qu'une** |
| O4 | Deux générations sans nouvelle mesure | rapport identique (hors horodatage) |
| O5 | `just check` | vert |

**Non testé par cette fiche** : la justesse des mesures elles-mêmes (fiches de banc) · la
qualité (chapeau `l1-05`).

## Notes / pistes

⚠ Le chiffre qui compte est **le run quotidien**, pas la reprise : trente minutes par jour
pour dix chips est un système qui vit ; six heures une fois est une nuit. Le mettre en
premier.

---

## Résumé de réalisation

*(à remplir avant de déplacer la fiche dans `fait/`)*

- **Ratio discrimination/seconde** : *(par modèle)*
- **Extrapolation quotidienne · reprise** : …
- **Écart extrapolation ↔ campagne réelle** : …
- **Commit(s)** : …
- **Date** : AAAA-MM-JJ
