---
id: l0-01.2
titre: Socle CLI — auto-découverte des commandes, codes de sortie figés, sites-validate/list
effort: S
categorie: pipeline
phase: O1
depends_on: [l0-01.1]
parent: l0-01
---

# [l0-01.2] — Socle CLI

## Objectif

Le mécanisme qui rend les fiches aval parallélisables : chaque commande = un module déposé
dans `cli/`, découvert automatiquement — `__main__.py` n'est plus jamais modifié ensuite.

## Périmètre (fichiers nommés)

- `src/tiny_wae/__main__.py` : boucle d'enregistrement (import des modules de `cli/`
  exposant `app` ou `register(app)` — choisir UNE convention et la documenter dans le
  docstring). La commande `version` existante survit.
- `src/tiny_wae/cli/exit_codes.py` : constantes figées `OK=0`, `FAILURE=1`, `USAGE=2`,
  `INCONCLUSIVE=3` — TOUS les CLIs du lot les importent (décision chapeau l0-01).
- `src/tiny_wae/cli/sites_validate.py` et `cli/sites_list.py` : wiring pur sur la
  validation de l0-01.1 ; `--json` pour sites-list ; logs STDERR.
- Taxonomie documentée (**9 sous-commandes, cf. chapeau — `smoke` n'en est PAS une, c'est
  un script**) : les 6 restantes n'existent PAS encore (pas de stubs) — la liste vit en
  commentaire dans `__main__.py`.

## ⚠ Ancrage dans le code réel (vérifié le 21/08/2026, base 061db8f)

**Vérifié dans le code, pas supposé :**

- `src/tiny_wae/__main__.py` existe : `app = typer.Typer(no_args_is_help=True, help=…)`, un
  callback `@app.callback() def _main()` **indispensable** (sans lui typer replie une app à
  commande unique et `version` deviendrait la commande racine — le docstring le dit), et
  `@app.command() def version()` qui écho `tiny_wae.__version__`, **valeur réelle `"0.1.0"`**
  (O4 confirmé). ⚠ **L'auto-découverte doit préserver ce callback ET la commande `version`.**
- `src/tiny_wae/cli/__init__.py` existe (docstring seule) — c'est le package à balayer.
- API réelle de l0-01.1, à câbler telle quelle :
  `adapters.config_io.load_sites(path: Path = DEFAULT_SITES_PATH) -> list[Site]`,
  `adapters.config_io.load_settings(...)`, `core.sites.validate_sites(sites: list[Site]) -> None`.
- ⚠ **DEUX exceptions à mapper sur `USAGE=2`, pas une** : `core.sites.SiteValidationError`
  (validation) **et** `adapters.config_io.ConfigError` (chargement : fichier absent, champ
  inconnu, valeur env non convertible). N'en attraper qu'une laisserait O3 rouge sur la moitié
  des configs cassées.
- O3 est déjà satisfaisable sans retoucher l0-01.1 : les messages de `SiteValidationError`
  nomment le site **et** le champ (`f"site {self.id} : lat={self.lat} hors bornes …"`).
  L'assertion portera sur ces deux chaînes littérales, pas sur une reformulation.

**Décisions prises à l'ancrage** (la fiche laissait explicitement le choix) :

1. **Convention d'enregistrement : `register(app: typer.Typer) -> None`** exposée par chaque
   module de `cli/` — retenue plutôt qu'un attribut `app`, parce qu'elle couvre aussi bien une
   commande simple qu'un sous-groupe et laisse le **nom** de la commande au module (`sites-validate`,
   tiret compris, que le nom de fonction Python ne peut pas porter).
2. **La fonction de découverte prend le package en paramètre** (défaut `tiny_wae.cli`). C'est ce
   qui rend O1 testable proprement : le test crée un package temporaire et le passe en argument,
   au lieu de déposer un fichier dans le `src/` installé puis d'espérer le nettoyer.
3. Fixture de config cassée → **`tests/fixtures/config/`** : `tests/fixtures/` est une zone
   partagée (l0-02.1 → `stac/`, l0-03.2 → `manifests/`, l0-03.5 → `cog/`), on reste dans son
   sous-dossier.

**Zone d'écriture EXCLUSIVE de cette fiche** — 3 agents travaillent en parallèle sur N1 :
`src/tiny_wae/__main__.py`, `src/tiny_wae/cli/**`, `tests/test_cli*.py`, `tests/fixtures/config/`.
⛔ Ne toucher NI `core/` (l0-03.1 en parallèle), NI `adapters/` (l0-03.2 en parallèle), NI
`pyproject.toml`, `justfile`, `config/`, `tests/test_config.py`.

## Définition de « terminé »

- [ ] Auto-découverte fonctionnelle ; `sites-validate`/`sites-list` branchées.
- [ ] Test de non-régression : `__main__.py` identique avant/après ajout d'un module.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | Module factice déposé dans `cli/` (tmp du test) | apparaît dans `--help` SANS modification de `__main__.py` (test automatisé) |
| O2 | `just run sites-validate` sur config livrée | exit 0 (`OK`) |
| O3 | Config cassée (fixture) | exit 2 (`USAGE`) ; STDERR contient **l'id du site** ET **le nom du champ** fautifs (assertion littérale sur les deux chaînes) |
| O4 | `just run version` | inchangé, `0.1.0` |
| O5 | `just check` | vert |

**Non testé** : les **6** sous-commandes restantes (fiches aval) — cf. taxonomie ci-dessus.

---
## Résumé de réalisation

**Fait** (agent Sonnet medium, worktree isolé, merge `8d000ab`) : auto-découverte des
commandes, codes de sortie figés, `sites-validate` et `sites-list`.

Livré : `cli/discovery.py`, `cli/exit_codes.py`, `cli/sites_validate.py`, `cli/sites_list.py`,
`__main__.py` (modifié une dernière fois), 2 fixtures de config cassée, **11 tests**.

| # | Verdict | Mesure |
|---|---|---|
| O1 | ✅ | Module factice dans un package temporaire → apparaît dans `--help` et s'exécute, **sans modification de `__main__.py`**. Test complémentaire : `__main__.py` ne référence nominativement aucun module de commande |
| O2 | ✅ | `just run sites-validate` → exit **0**, STDERR « 25 site(s) valide(s) » |
| O3 | ✅ | Config cassée → exit **2** ; STDERR `site X01 : lat=95.0 hors bornes [-90.0, 90.0]` — contient **l'id ET le champ**. **2 fixtures** couvrant les deux exceptions imposées par l'ancrage (`SiteValidationError` et `ConfigError`) |
| O4 | ✅ | `just run version` → `0.1.0`, exit 0 — inchangé |
| O5 | ✅ | `just check` vert |

**Décisions prises en cours de fiche** : convention `register(app)` (fixée à l'ancrage, non
rouverte) ; paramètre nommé `package_name` ; deux fixtures plutôt qu'une, pour couvrir les deux
exceptions ; `# noqa: B008` inline sur les `Option()` en défaut d'argument (idiome typer) plutôt
que de toucher `pyproject.toml`, hors zone.

**Vérifié par l'orchestrateur après merge** : callback `_main` et commande `version` préservés,
`discover_commands(app)` appelé au bon endroit ; aucun fichier hors zone touché.
⚠ **Correction d'un chiffre de l'agent** : il annonçait « 8 nouveaux tests », en oubliant un de
ses deux fichiers. Le compte réel est **11** (`test_cli_commands.py` 8 + `test_cli_discovery.py`
3) ; son total de 26 était juste.
