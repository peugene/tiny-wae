---
id: l0-06
titre: "[CHAPEAU] Emballage CWL — tools + workflow, validation locale"
effort: M
categorie: chapeau
phase: O3
depends_on: []       # E-c : un CHAPEAU n'ordonne rien (après l0-03/l0-05 — cf. corps)
parent:
subtasks: [l0-06.1, l0-06.2]
---

# [l0-06] — CHAPEAU : Emballage CWL

> Fiche chapeau, non dispatchable, `depends_on: []` (E-c : un chapeau n'ordonne rien).
> **Vient après l0-03 et l0-05** (il emballe `search`, `ingest` et `update`) — les arêtes
> réelles sont portées par les sous-tâches.

## Contexte et décisions (ne pas rouvrir)

Q-D tranchée (G7) : **validation locale `cwltool` uniquement** — l'enregistrement PID-FLOW
réel est hors lot (fiche future, version PID-FLOW cible à nommer alors). Liste fermée :
3 CommandLineTools (`search`, `ingest`, `update`) + 1 Workflow (search → ingest, chaîné
par l'enveloppe JSON — c'est pour ça qu'`ingest --acquisitions` existe). CWL v1.2,
`baseCommand: python -m tiny_wae <cmd>` (écart cwl-assets documenté). `pid:Milestone` :
RETIRÉ du périmètre (scope creep pointé en revue v2 — le consommateur, l'IHM PID-FLOW,
est hors lot ; à réintroduire dans la fiche d'enregistrement future). Recette `just cwl`
(validation statique) branchée dans `just check` — impact assumé : `cwltool` entre dans
l'environnement de tous les worktrees (dit ici, conformément à la règle « le gate
appartient à la fiche qui l'ajoute »).

## Sous-tâches

| Fiche | Effort | Objet |
|---|---|---|
| l0-06.1 | M | search.cwl + ingest.cwl + workflow.cwl + README + just cwl dans check |
| l0-06.2 | S | update.cwl + run local comparatif |

**Non testé** : exécution par un worker PID-FLOW réel (hors lot, G7).
