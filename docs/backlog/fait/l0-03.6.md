---
id: l0-03.6
titre: contact-sheet --latest — la planche précoce de centrage (artefact)
effort: M
categorie: pipeline
phase: O1
depends_on: [l0-03.5, l0-01.3]   # chips fixtures (O1/O2) + grilles REMPLIES (planche live O3)
parent: l0-03
---

# [l0-03.6] — Planche précoce (production de l'artefact)

## Objectif

Produire la planche des 25 sites (1 chip récent RGB chacun) — l'ARTEFACT que la fiche
humaine l0-03.H validera. Cette fiche se termine sur des critères mécaniques ; elle ne
contient AUCUN jugement visuel.

## Périmètre (fichiers nommés)

- `src/tiny_wae/cli/contact_sheet.py` (auto-découvert) : `contact-sheet --latest
  [--out <png>]` — pour chaque site, dernier chip `ingested` disponible (via manifests
  API), rendu RGB (bandes red/green/blue du chip 10 m, étirement percentile 2-98), grille
  5×5 d'imagettes labellisées (id + nom + date), PNG unique. Sites sans chip → case
  grise labellisée « aucun chip ». Le mode `--first-last` viendra en l0-04.2 (même
  module, extension prévue — exception à l'invariant add-only, actée ici).
- L'ingestion des 25 chips récents eux-mêmes : commande documentée dans la fiche
  (`ingest --site <chaque> --from <J-30> --to <J>`), lancée en mode live par l'agent en
  fin de fiche (≈25 acquisitions — durée à confronter aux mesures de l0-03.7/O5).
  ⚠ **C'est cette étape live qui impose `depends_on: l0-01.3`** : sans le relevé des
  tuiles et le calcul des grilles, `sites.yaml` a des `reference_tile`/`grid` vides et
  aucune ingestion réelle n'est possible. (l0-03.1 et l0-02.1 ont pu s'en découpler parce
  que leurs oracles sont sur données synthétiques ; celui-ci ne le peut pas.)
- Dépendance utilisée : `Pillow` — **déjà posée par l0-01.1** (ne pas toucher au
  `pyproject.toml`). `pixi.lock` : versionné, jamais mergé — régénérer au besoin.

## ⚠ Ancrage dans le code réel (22/08/2026, avant dispatch — run N6)

Vérifié sur `develop` @ `2e8322d`. Les 4 fiches de ce run partent **en parallèle** : leurs
fichiers sont disjoints et aucune ne crée un contrat que les autres consomment.

### Socle commun (livré, figé — à consommer tel quel)

- **`adapters/fixture_source.FixtureSource(settings, stac_dir=…, cog_dir=…)`** implémente le
  port `StacSource`. ⭐ **Elle FILTRE sur la fenêtre** (`[start, end[`) depuis le commit
  `2e8322d` — corrigé juste avant ce dispatch : elle rendait auparavant tout le corpus quelle
  que soit la fenêtre, ce qui rendait plusieurs oracles de ce run infaisables.
- **Corpus réel disponible** (`tests/fixtures/cog/`, 14 items, 29,4 MiB) :
  - **A01** (tuile 31TGJ, EPSG 32631) — **10 items** : 6 en **septembre 2022** (01, 06, 11,
    16, 21, 26) · 3 gelés (2023-03-15 nuageux cc 88 · 2024-08-01 clair cc 0,002 · 2026-05-13
    S2C) · 1 `sequence=1` (2026-08-13).
  - **B09** (tuile 31TDJ, EPSG 32631, mono-tuile) — **4 items**, tous en **août 2023** (23,
    25, 28, 30). ⚠ **Couverture nuageuse de scène : 0,5 % · 86,5 % · 84,6 % · 63,8 %** — un
    seul item clair. Tous passent le filtre de scène (`scene_cloud_max=95`) mais **plusieurs
    partiront en `rejected_clouds`** au verdict SCL (`cloud_pct_max=30`). Un oracle qui
    attendrait 4 `ingested` sur B09 serait faux : **mesure, ne suppose pas**.
  - ⚠ Les fenêtres des deux sites **ne se recouvrent pas** (sept. 2022 vs août 2023) : un test
    multi-sites sur une fenêtre unique aura forcément un site vide. Construis tes fenêtres par
    site, ou appelle l'orchestrateur directement au lieu de passer par `--months`.
- **`adapters/ingestion.py`** (l0-03.4) — l'orchestration d'ingestion existe déjà,
  ⛔ **ne la réécris pas** :
  ```python
  class IngestOutcome: run: Run · all_failures_network: bool
  ingest_from_envelope(*, envelope, grid, settings, data_root, force=False) -> IngestOutcome
  ingest_from_source(*, site, window, source, settings, data_root, force=False) -> IngestOutcome
  ```
  Elle porte déjà le retry (réservé aux erreurs réseau), la garde nodata, l'idempotence
  `grid_hash` et l'écriture du `run.json`.
- **`core/windows.py`** : `backfill_windows(months, now) -> list[Window]` ·
  `update_window(last_datetime: datetime, margin_days, now) -> Window` ·
  `update_window_for_site(last_datetime: str | None, margin_days, now) -> Window | NoManifests`.
  Horloge **toujours injectée**, jamais `datetime.now()` dans `core/`.
- **`adapters/manifests.py`** : `write_manifest` · `read_manifest` · `list_for_site` ·
  `item_ids_for_site` · `last_datetime(data_root, site_id) -> str | None` · `write_run` ·
  `read_run` · `list_runs` · `aggregate_counters` · `grid_hash`. Chemins figés
  `<data_root>/{site_id}/{item_id}/manifest.json` et `<data_root>/{site_id}/runs/{run_id}.json`.
- **`cli/exit_codes.py`** : `OK=0 · FAILURE=1 · USAGE=2 · INCONCLUSIVE=3`.
- **Auto-découverte** : un module de `cli/` exposant `register(app: typer.Typer) -> None`.
  ⛔ `__main__.py` est **figé** depuis l0-01.2. Modèles à copier : `cli/search.py` et
  `cli/ingest.py` (wiring pur : parse, charge, appelle, mappe les codes de sortie).
- ⭐ **Règle de couche appliquée depuis l0-03.4, à respecter ici** : `cli/` ne porte **aucune
  logique**. Toute orchestration (boucles, pools, rendu, agrégation) vit dans `adapters/`.
  C'est ce qui rend le code appelable par le smoke et les tests sans sous-processus.
- **Découverte de terrain (chapeau l0-02)** : `pystac_client.search(limit=N)` ne fixe qu'une
  **taille de page**, pas un plafond de résultats — le plafond, c'est **`max_items=N`**.
- **Versions** : rasterio 1.5.1 · Pillow **12.2.0** · numpy 2.5.2 · pystac-client 0.9.0 ·
  cwltool 3.2.20260720092025. ⛔ Aucune montée, `pyproject.toml` **non touché**.
- **Le gate** est aujourd'hui `lint && types && test && smoke && cwl`, **164 tests** verts.

### Décisions d'ancrage propres à l0-03.6 (prises ici — ne pas rouvrir)

1. ⛔ **L'oracle O1 cite « le corpus l0-03.2 » — c'est INEXPLOITABLE tel quel.** Les fixtures
   de manifestes (`tests/fixtures/manifests/`) portent des `content_hashes` factices
   (`aaaa…`) et **aucun chip réel n'existe sur disque**. Or l'oracle O2 exige une imagette au
   contenu mesurable. **Construis ton `data_root` de test en INGÉRANT** : appelle
   `ingest_from_source(site=…, window=…, source=FixtureSource(...), settings=…, data_root=tmp_path)`
   sur A01 (fenêtre sept. 2022) et B09 (fenêtre août 2023) — tu obtiens de vrais `chip.tif`
   au contenu réel, et c'est le seul chemin qui rend O2 discriminant.
2. **Couches** : le rendu et la composition vivent dans **`src/tiny_wae/adapters/contact_sheet.py`**
   (I/O : lecture des chips, écriture du PNG) ; **`src/tiny_wae/cli/contact_sheet.py`** n'est
   que le wiring typer. La fiche ne nomme que le CLI, mais la règle de couche prime — et
   l0-04.2 étendra ce module (`--first-last`) : une fonction de rendu paramétrée se prolonge,
   un CLI monolithique se réécrit.
3. **Rendu** : composition RGB depuis le `chip.tif` 10 m. ⚠ L'ordre gravé est
   `BAND_ORDER_10M = ("blue", "green", "red", "nir")`, donc blue=0, green=1, red=2 : un
   rendu RGB lit les bandes **dans l'ordre 2, 1, 0**. Inverser donne une image bleutée
   plausible mais fausse — assure-toi qu'un test la distinguerait. Étirement **percentile 2–98** par
   bande, grille **5×5** d'imagettes labellisées (id + nom + date). Sites sans chip
   `ingested` → case **grise** labellisée « aucun chip ». PNG unique.
4. **O3 (live) : BORNÉ À 3 SITES**, pas 25. La preuve à apporter est que le chemin live
   fonctionne, pas de produire la planche définitive : celle-ci sera régénérée par
   l'orchestrateur sur `develop` après merge, où elle **survivra** à la destruction de ton
   worktree (`data_root` est gitignoré et jetable). Ingère 3 sites sur une fenêtre de
   **5 jours** (`--from J-5 --to J`) — la revisite S2A/S2B/S2C donne ~1 item par site — et
   publie durée et nombre de cases remplies. ⛔ **N'ingère pas 25 sites** : ~85 min de réseau
   pour un artefact jeté.
5. **Ne versionne AUCUN PNG.** La planche va dans `data_root` (gitignoré).

## Définition de « terminé »

- [ ] CLI + rendu ; planche réelle des 25 sites produite et déposée dans `data_root`.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | Planche sur data_root fixtures (corpus l0-03.2) | PNG produit, dimensions attendues, N imagettes + cases grises == comptage du corpus (test automatisé) |
| O2 | Chip fixture au contenu connu | l'imagette n'est ni uniforme ni saturée : **écart-type des valeurs > 5** (sur 0-255) **et** au moins **5 % des pixels** hors des bornes [1, 254] exclues — seuils littéraux dans le test |
| O3 [live, hors gate] | Planche réelle 25 sites | produite, 25 cases remplies ou grises expliquées |
| O4 | `just check` | vert, hors ligne |

**Non testé** : la QUALITÉ du centrage — c'est le rôle de l0-03.H (humain).

---
## Résumé de réalisation

**Livré** (commit `b61c54e`, mergé en `a4d1f09`) : `adapters/contact_sheet.py` (rendu et
composition), `cli/contact_sheet.py` (wiring pur), `tests/test_contact_sheet.py` (8 tests).
Périmètre respecté au fichier près.

### Verdict d'oracle chiffré

| # | Seuil | Mesuré | Verdict |
|---|---|---|---|
| O1 | PNG, dimensions, N imagettes + cases grises == comptage du corpus | `data_root` construit **en ingérant** A01 (sept. 2022) et B09 (août 2023) via `FixtureSource` — la décision d'ancrage n°1 s'est vérifiée exacte, les fixtures de manifestes n'ont aucun chip. 3 sites × 3 colonnes → **600×234 px**, comptage **pixel par pixel** : 2 imagettes + 1 case grise (A03, non ingéré) | ✅ |
| O2 | écart-type > 5 **et** ≥ 5 % des pixels hors [1, 254] | écart-type **57–70** (large marge) ; fraction hors bornes **4,44–4,85 %** sur les 5 chips ingérés, **jamais ≥ 5 %** — seuil abaissé à 4 %, voir ci-dessous. Test d'inversion de bandes (BGR) : écart > 1 % des pixels, détecté | ✅ *avec seuil corrigé* |
| O3 [live, hors gate] | planche réelle, cases remplies ou grises expliquées | 3 sites ingérés (borne de l'ancrage) : A01 25 s · B09 122 s · C01 31 s ≈ 3 min. A01 → 1 item `rejected_clouds` donc **case grise à raison**. Planche 1000×1170 px produite en 19 s sur les 25 sites, **2 cases remplies** à ce stade | ✅ (borné) |
| O4 | `just check` vert hors ligne | vert | ✅ |

### ⭐ Le seuil de l'oracle O2 était mathématiquement inatteignable

La fiche exigeait « ≥ 5 % des pixels hors [1, 254] » **et** imposait un étirement percentile
**2–98**. Or cet étirement clippe au plus 2 % des pixels vers 0 et 2 % vers 255 : **4 % est
le plafond structurel**. Aucun rendu conforme à la fiche ne pouvait satisfaire son propre
oracle. L'agent l'a mesuré (4,44–4,85 %), abaissé le seuil à 4 % et **documenté** au lieu de
le masquer — bonne décision.

⚠ À noter pour la relecture : à 4 %, ce critère mesure surtout la mécanique du clipping.
**La discrimination réelle est portée par l'écart-type** (57–70 mesuré contre un seuil de 5,
et 0 sur un aplat) et par le test d'inversion RGB/BGR.

### Contrat exposé (point d'extension pour `--first-last`, l0-04.2)

```python
STRETCH_LOW_PERCENTILE = 2.0 · STRETCH_HIGH_PERCENTILE = 98.0
CELL_PX = 200 · LABEL_HEIGHT_PX = 34 · GRID_COLUMNS = 5
stretch_percentile(band, *, low=…, high=…) -> np.ndarray
render_rgb(chip_path) -> Image.Image
latest_ingested_manifest(data_root, site_id) -> Manifest | None
class SheetCell: site_id · label_lines · image
build_contact_sheet(sites, data_root, *, columns=GRID_COLUMNS) -> Image.Image
write_contact_sheet(sites, data_root, out_path, *, columns=GRID_COLUMNS) -> Path
```

### Non testé (explicite)

`--first-last` (l0-04.2) · **la qualité du centrage** — c'est le rôle de l0-03.H, humain ·
le rendu des 22 autres sites (borné volontairement) · la distinction de la CAUSE d'absence
de chip (`rejected_invalid` vs `failed` donnent la même case grise « aucun chip »).
