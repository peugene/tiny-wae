---
id: l0-R1
titre: "[RÉTROACTIVE] Durcissement de la livraison PID-FLOW — conventions CWL + contrat de la wheel"
effort: "—"
categorie: documentaire
phase: O3
depends_on: []
parent:
subtasks: []
---

# [l0-R1] — RÉTROACTIVE : durcissement de la livraison PID-FLOW

> 📘 **Fiche documentaire, écrite APRÈS coup.** Elle ne demande aucun travail : elle
> enregistre cinq commits déjà livrés, réalisés hors du cycle `maturation → a-faire →
> en-cours → fait`. Rien à dispatcher, `depends_on: []`, aucune sous-tâche.
> **Constat d'origine** : post-revue 1, points B1 à B3.

## Pourquoi cette fiche existe

Entre le 22/08/2026 07:37 et 09:21, cinq commits ont modifié en profondeur les artefacts
CWL et le contrat de packaging du projet. Ce travail est de qualité et abondamment motivé
— **dans les corps de commit**. Mais le backlog, dont la règle est que « le dossier fait
foi », n'en portait aucune trace : les fiches `l0-06.1` et `l0-06.2`, closes en `fait/`,
décrivaient encore une arborescence `cwl/` et un `baseCommand: [python, -m, tiny_wae, …]`
qui n'existent plus.

Un lecteur du backlog dans six mois n'aurait pas su que ce refactor avait eu lieu, ni
pourquoi le `glob` du tool `search` est un littéral plutôt qu'une expression — alors que
c'est précisément le genre de décision qu'on re-découvre à ses dépens.

⚠ **Cette fiche est un précédent, pas une pratique.** Écrire une fiche après le code
inverse la règle du projet (« les décisions se prennent en amont »). Elle se justifie ici
parce que le travail porte des décisions structurantes qui seront relues au Lot 1. Le
réflexe correct reste : une contrainte externe arrive en cours de lot → **elle produit une
fiche avant d'être implémentée**, même courte.

## Ce qui a été livré (déjà en place sur `develop`)

Cinq commits, du plus ancien au plus récent :

| Commit | Objet |
|---|---|
| `13de5f0` | Retrait de `InlineJavascriptRequirement` des 3 tools |
| `e355121` | Retrait de `EnvVarRequirement` et de l'input `data_root` |
| `24b564c` | Passage à la structure et aux conventions du cwl-store PID-FLOW |
| `208f79b` | `baseCommand` sur le point d'entrée console `tiny-wae` |
| `24680d1` | Complétion de `[project.dependencies]` — la wheel était incomplète |

**Bilan mesuré** (`git diff --stat d837c76 24680d1`, comptage `def test_`) : 13 fichiers
touchés hors `docs/`, **+956 / −458 lignes** · `tests/test_cwl.py` passe de **10 à 16
tests** · `tests/test_packaging.py` créé (**2 tests**) · total du projet **230 → 238**
fonctions de test (**243 collectés** par pytest, la paramétrisation en ajoutant 5).

### 1. Arborescence : `cwl/` devient `assets/cwl/`

```
assets/cwl/workflows/tiny-wae/1.0/workflow.cwl
assets/cwl/tools/{search,ingest,update}/1.0/tool.cwl
```

Hiérarchie symétrique du cwl-store PID-FLOW (dépôt `cwl-assets`, ADR-0010 / EVO-42).
**L'identité d'un artefact est le nom de son RÉPERTOIRE** — tous les fichiers s'appellent
`tool.cwl` ou `workflow.cwl`, en version `1.0`. Les 3 tools sont des tools **partagés** ;
le workflow les référence en `run: ../../../tools/<nom>/1.0/tool.cwl`.

### 2. Trois contraintes PID-FLOW, découvertes en séquence

Chacune a le même profil, et c'est **la leçon de fond de cette fiche** :
`cwltool --validate` reste **vert** alors que PID-FLOW refuserait l'artefact, ou pire,
l'exécuterait en perdant silencieusement une sortie. **L'exécuteur de développement est
plus permissif que celui de production.**

- **`InlineJavascriptRequirement` non supporté.** L'exigence n'apportait rien : les tools
  n'utilisaient que des références de paramètre, sous-ensemble toujours disponible sans
  exigence déclarée.
- **`EnvVarRequirement` non supporté** → l'input `data_root` disparaît avec lui (il n'aurait
  plus par quel moyen atteindre le process). `TINY_WAE_DATA_ROOT` est posée dans
  l'environnement du worker, hors CWL. Corollaire local mesuré : `cwltool` n'expose au job
  qu'un environnement minimal — une variable posée dans le shell appelant **n'atteignait
  pas** le job. D'où `--preserve-environment TINY_WAE_DATA_ROOT` dans `just cwl-run`, qui
  reproduit en local le comportement du worker.
- **⛔ Plus aucune expression CWL, pas même une référence de paramètre.** Le dépôt
  `cwl-assets` porte le constat (tool `AppendToFile`, TOOL-6) : un `glob $(inputs.…)` « ne
  matchait rien » côté PID-FLOW et **la sortie était perdue SANS erreur**. Le `glob` du tool
  `search` devient le littéral `"acquisitions.json"`, tenu synchronisé avec le défaut de
  l'input `json_out` par un test dédié — le lien que l'expression portait gratuitement.

### 3. Conventions de contenu (cwl-assets)

- `label` = **nom de l'artefact** (c'est lui qui identifie la brique côté IHM) ; la phrase
  descriptive passe en `doc`. `label` + `doc` sur chaque input, output et step.
- `hints: SoftwareRequirement / package: python` sur chaque tool. **Sans cette capability,
  le worker ne prend pas la tâche et elle reste en attente sans erreur** — un échec muet de
  plus.

### 4. `baseCommand` : le point d'entrée console

`[project.scripts] tiny-wae = "tiny_wae.__main__:app"` génère un exécutable `tiny-wae` dans
le `bin/` de l'environnement d'installation ; les 3 tools l'appellent en
`baseCommand: [tiny-wae, <sous-commande>]`.

**Pourquoi pas `python -m tiny_wae`** : les deux formes échappent à la réécriture du worker
(qui ne vise que les `baseCommand` pointant un `.py` relatif), donc l'exécutable est résolu
sur le PATH du job dans les deux cas. Mais `python` est le nom le plus surchargé du système :
résolu sur un autre interpréteur que celui du venv, il **démarre puis échoue** sur
`No module named tiny_wae` — une erreur qui parle d'un module alors que le problème est un
interpréteur. `tiny-wae` est unique au projet : absent du PATH, il échoue franchement.

Mesuré : `cwltool` transmet au job l'intégralité du PATH parent (seuls `HOME`, `PWD` et
`TMPDIR` sont recréés), donc le `bin/tiny-wae` de l'env pixi est trouvé sans rien déclarer.

### 5. Le contrat de la wheel était faux

`[project.dependencies]` ne déclarait que `typer`/`pyyaml`/`httpx`/`pystac-client`. Les
natifs (`rasterio`, `pyproj`, `pillow`) ne vivaient que dans `[tool.pixi.dependencies]`,
`numpy` nulle part (transitif de rasterio sous conda), et `affine` non plus alors qu'il est
importé **directement** par `core/geometry.py` et `adapters/chips.py`.

Conséquence mesurée sur un venv nu : `pip install` réussit, puis `tiny-wae version` meurt sur
`ModuleNotFoundError: No module named 'numpy'`. **Le paquet s'installe et casse à l'import —
c'est-à-dire chez le worker PID-FLOW, au premier run.** Invisible en dev, puisque pixi
installe aussi la liste conda.

Ajoutés au contrat : `numpy>=2.0`, `rasterio>=1.4`, `affine>=2.4`, `pyproj>=3.6`,
`pillow>=11.0`. Les trois natifs restent **aussi** dans `[tool.pixi.dependencies]` : là c'est
le choix de la **source** pour l'env de dev (builds conda-forge), ici c'est le **contrat de
livraison**.

## Ce qui garde tout ça (les tests, pas la vigilance)

Le point commun de ces cinq commits : **aucun de ces défauts n'est visible du gate
habituel**. `just cwl` reste vert avec une exigence non supportée ; `just check` reste vert
avec une wheel incomplète. Chaque contrainte a donc reçu son garde-fou automatique, et
chacun a été **vérifié par mutation** (cassé exprès → rouge, restauré → vert) :

| Garde-fou | Ce qu'il attrape |
|---|---|
| `test_aucun_cwl_ne_declare_d_exigence_non_supportee_par_pid_flow` | `_FORBIDDEN_REQUIREMENTS`, sous les **deux** formes CWL (clé de map, entrée `{class: …}` de liste), récursivement jusque dans les steps |
| `test_aucun_cwl_ne_declare_d_input_data_root` | réapparition de l'input supprimé |
| `test_aucune_expression_cwl_dans_les_fichiers` | retour d'un `$(…)` n'importe où |
| `test_search_glob_litteral_colle_au_default` | dérive entre le glob littéral et le défaut de `json_out` |
| `test_arborescence_pid_flow`, `test_class_coherente_avec_le_repertoire` | `.cwl` hors convention, classe incohérente avec son répertoire |
| `test_label_egale_le_nom_de_l_artefact` | `label` ≠ nom du répertoire |
| `test_chaque_tool_exige_la_capability_python` | capability retirée → tâche jamais prise par le worker |
| `test_workflow_reference_des_tools_partages_existants` | `run:` vers une version inexistante |
| `test_le_console_script_est_declare_dans_le_paquet` | `baseCommand` pointant dans le vide après renommage de l'entrée |
| `tests/test_packaging.py` (2 tests) | tout module tiers importé par `src/` appartient à une distribution déclarée au contrat (lecture par `ast` + `packages_distributions()`, jamais une table écrite à la main) ; et tout ce que `[tool.pixi.dependencies]` déclare est aussi au contrat |

C'est `test_packaging.py` qui a débusqué `affine`, oublié par l'auteur du correctif.

## Vérifications effectuées à l'époque

- `just cwl` : **4/4** « is valid CWL » (cwltool 3.2.20260720092025) ; `just check` vert.
- Runs réels du workflow `tiny-wae` sur A01 après **chacun** des refactors :
  `found_stac=3`, `found_tile=3`, 3 manifestes écrits sous `TINY_WAE_DATA_ROOT`, chaînage
  `search → ingest` intact, enveloppe capturée par le glob littéral.
- Wheel : en venv nu, `pip install` puis `tiny-wae --help` liste les 9 sous-commandes —
  donc toute la chaîne d'imports des adapters passe, **sans conda côté worker** (wheels
  manylinux : GDAL embarqué dans rasterio, PROJ dans pyproj).

## ✅ Revérifié après le refactor — constat B3 de la post-revue 1, CLOS

Les deux oracles ci-dessous avaient bien été exécutés en leur temps, mais **sur les CWL
d'avant**. Les runs post-refactor ne prouvaient que « ça tourne » — ni l'équivalence, ni
l'idempotence. Or le refactor a touché exactement ce qui pouvait les casser : le chemin
d'exécution (`baseCommand`) et la récupération des sorties (le `glob`).

**Rejoués le 2026-08-22 depuis `assets/cwl/`, réseau réel, sur des racines vierges :**

| Oracle | Attendu | Mesuré | Verdict |
|---|---|---|---|
| `l0-06.1 / O2` | ensembles `(item_id, statut)` identiques CWL vs CLI direct | **3 items de part et d'autre, ensembles identiques** | ✅ |
| `l0-06.2 / O2` | 2ᵉ run : `ingested == 0` ET `assets_read == 0` | `status=up_to_date ingested=0 assets_read=0` | ✅ |

**Détail `l0-06.1 / O2`** — A01, fenêtre `2026-01-01 → 2026-01-10`, deux racines vierges
(`/tmp/o2-cwl` via `workflows/tiny-wae/1.0/workflow.cwl`, `/tmp/o2-cli` via
`just run search` puis `just run ingest`). Les deux branches rendent `found_stac=3`,
`found_tile=3`, et le MÊME ensemble `(item_id, statut)` lu dans les manifestes :

```
('S2C_31TGJ_20260103_0_L2A', 'ingested')
('S2A_31TGJ_20260105_0_L2A', 'rejected_clouds')
('S2B_31TGJ_20260108_0_L2A', 'rejected_clouds')
```

**Détail `l0-06.2 / O2`** — racine vierge amorcée par `ingest` sur `2026-08-02 → 2026-08-21`
(`found_stac=5`, `skipped_scene_cloud=1`, `found_tile=4`, `ingested=4`), puis deux runs
successifs de `tools/update/1.0/tool.cwl` :

| run | résultat |
|---|---|
| 1ᵉʳ (rattrapage de la fenêtre laissée ouverte) | `status=up_to_date ingested=0 assets_read=1` |
| **2ᵉ — celui que l'oracle mesure** | `status=up_to_date ingested=0 **assets_read=0**` |

L'`assets_read=1` du 1ᵉʳ run est attendu : la marge `incremental_margin_days` fait
réexaminer un item à cheval sur la fenêtre d'amorçage. Le 2ᵉ run, lui, ne lit plus rien —
c'est l'idempotence recherchée.

## Reste côté ops (hors code)

Documenté dans `assets/cwl/README.md` : **la wheel doit être installée dans le venv du
worker, et le `bin/` de ce venv doit être sur le PATH des jobs.** Sans quoi
`baseCommand: [tiny-wae, …]` échoue à la résolution.

## À reporter dans les fiches closes

`l0-06.1` et `l0-06.2` en `fait/` restent à corriger sur deux points (post-revue 1, B2) :
leur périmètre nomme `cwl/*.cwl` et un `baseCommand: [python, -m, tiny_wae, …]`. Un
post-scriptum daté renvoyant vers cette fiche suffit — **ne pas réécrire l'historique** des
fiches closes, ajouter.

## Définition de « terminé » — remplie le 2026-08-22

Purement documentaire, trois conditions, toutes tenues :

- **(a)** les deux O2 ont été rejoués depuis `assets/cwl/` et consignés ci-dessus — ✅ tous
  deux au vert ;
- **(b)** le post-scriptum a été ajouté à `l0-06.1` et `l0-06.2` en `fait/` (ajout daté, pas
  de réécriture de l'historique des fiches closes) — ✅ ;
- **(c)** déplacement en `fait/` — ✅.

Aucun code écrit, conformément à la nature documentaire de la fiche. Deux chiffres du bilan
ont été corrigés à la relecture (`test_cwl.py` : 10 → 16 et non 8 → 16 ; total : 230 → 238
et non 213 → 238), vérifiés par `git diff --stat d837c76 24680d1` et comptage `def test_`.

**Reste ouvert, hors de cette fiche** : la vérification côté ops (wheel installée dans le
venv du worker, `bin/` de ce venv sur le PATH des jobs) — elle ne peut pas se mesurer depuis
le dépôt.
