---
id: l0-06.1
titre: search.cwl + ingest.cwl + workflow.cwl + README + recette just cwl dans le gate
effort: M
categorie: outillage
phase: O3
depends_on: [l0-02.2, l0-03.4]   # emballe le CLI `search` (l0-02.2) ET `ingest` (l0-03.4)
parent: l0-06
---

# [l0-06.1] — Tools CWL search/ingest + workflow

## Périmètre (fichiers nommés)

- `cwl/search.cwl`, `cwl/ingest.cwl` (CommandLineTool, **CWL v1.2 en 1re ligne**,
  `baseCommand: [python, -m, tiny_wae, <cmd>]`) ; `cwl/workflow.cwl` : step search →
  fichier enveloppe JSON → step ingest `--acquisitions` (le chaînage repose sur
  l'invariant STDOUT-JSON de l0-02.2).
- `cwl/README.md` : conventions, écart assumé vs cwl-assets (package installé, pas de
  scripts rsyncés), périmètre (PAS d'enregistrement server, PAS de pid:Milestone — cf.
  chapeau), commandes de validation et de run local.
- `justfile` : recette `cwl` (`cwltool --validate` sur tous les `.cwl`) ajoutée à
  `check`. Dépendance utilisée : `cwltool` — **déjà posée par l0-01.1** (ne pas toucher au
  `pyproject.toml`). ⚠ Conséquence assumée : à partir de cette fiche, `just check` exige
  `cwltool` dans tous les worktrees. `pixi.lock` : versionné, jamais mergé.

## ⚠ Ancrage dans le code réel (22/08/2026, avant dispatch — run N5, en parallèle de l0-03.5)

Vérifié sur `develop` @ `5e4ab01`. Cette fiche part **en parallèle de l0-03.5** : ses deux
dépendances (l0-02.2 et l0-03.4) sont en `fait/`, ses fichiers sont disjoints de ceux de
l0-03.5, et aucune des deux ne crée un contrat que l'autre consomme.

### Faits vérifiés

- ⭐ **`cwltool` EST bien déjà posé** — le fait de la fiche est exact, vérifié :
  `pyproject.toml` ligne 55, dans `[tool.pixi.feature.dev.pypi-dependencies]`, avec le
  commentaire « pas de build conda-forge compatible python 3.12 disponible — PyPI ».
  Version installée : **3.2.20260720092025**, `pixi run cwltool --version` répond.
  ⛔ **Ne touche donc PAS `pyproject.toml`.**
- **Options RÉELLES de `search`** (relevées par `just run search --help`) :
  `--site <str>` (requis) · `--from <str>` YYYY-MM-DD (requis) · `--to <str>` (requis) ·
  `--json <path>` (écrit l'enveloppe dans un fichier **au lieu de STDOUT**) ·
  `--sites-path <path>` (défaut `config/sites.yaml`) · `--settings-path <path>` (défaut
  `config/settings.yaml`).
- **Options RÉELLES de `ingest`** (relevées par `just run ingest --help`) :
  `--acquisitions <path>` (enveloppe JSON déjà produite — **c'est le point de chaînage**) ·
  `--site <str>` · `--from <str>` · `--to <str>` · `--force` · `--sites-path <path>` ·
  `--settings-path <path>`. ⚠ **Il n'y a AUCUNE option de `data_root`** : il vient de
  `settings.data_root`, surchargeable par la variable d'environnement **`TINY_WAE_DATA_ROOT`**
  (mécanisme de surcharge `TINY_WAE_*` d'`adapters/config_io.py`). C'est le levier de
  l'oracle O2 — en CWL, il passe par un `EnvVarRequirement`.
- **Codes de sortie de `ingest`** : `0` · `1` (≥ 1 failed avec au moins un succès) ·
  `2` (config/usage, dont grille non calculée) · `3` (tous les échecs réseau, aucun abouti).
  Un CWL qui ne déclare que `successCodes: [0]` fera échouer le workflow sur un `1` légitime :
  décide explicitement de ce que tu déclares, et écris-le dans le README.
- **Le dossier `cwl/` n'existe pas encore** — tu le crées.
- **`just check`** enchaîne aujourd'hui `lint && types && test && smoke`. Tu y ajoutes `cwl`.
  ⚠ Le `justfile` est à toi sur ce run : l'autre agent a interdiction d'y toucher.
- **Versions** : cwltool 3.2.20260720092025 · rasterio 1.5.1 · pystac-client 0.9.0.

### Décisions d'ancrage (prises ici — ne pas rouvrir)

1. **`baseCommand: [python, -m, tiny_wae, <cmd>]`** comme le veut le chapeau. ⚠ Ce `python`
   en dur n'enfreint PAS la règle façade : un `.cwl` est destiné à un exécuteur externe
   (worker PID-FLOW), il ne peut pas connaître `just`. La règle façade s'applique aux
   recettes, à la doc et aux prompts — écris cette justification dans `cwl/README.md`,
   pour que personne ne « corrige » ces fichiers plus tard.
2. **La recette `just cwl` ne fait QUE de la validation statique** :
   `cwltool --validate` sur les 3 fichiers, et rien d'autre. ⛔ Aucune exécution réelle dans
   le gate (l'oracle O2 est explicitement « local, hors gate » : il fait du réseau).
3. ⭐ **L'oracle O3 mérite mieux qu'une redite.** L'invariant « STDOUT du step search = JSON
   parsable seul » est déjà couvert par les tests de l0-02.2. Ce qu'il faut tester ICI, et
   qui n'existe nulle part ailleurs, c'est la **cohérence entre les `.cwl` et les CLI réels** :
   un test pytest qui charge les 3 fichiers CWL (ce sont des YAML — `yaml.safe_load`) et
   vérifie que chaque `inputBinding.prefix` déclaré correspond à une option réellement
   exposée par le CLI correspondant. Motif : le jour où une option du CLI est renommée, la
   validation `cwltool --validate` restera **verte** (elle ne connaît pas nos CLI) et le
   workflow cassera silencieusement en production. C'est le seul test de cette fiche qui
   échouerait sur du code faux.
4. **Chaînage du workflow** : `search` écrit son enveloppe via `--json <fichier>` (et NON par
   capture de STDOUT — c'est plus robuste en CWL et l'option existe pour ça), le step
   `ingest` la consomme par `--acquisitions`. Déclare l'enveloppe en `outputBinding.glob`.
5. **`cwl/README.md`** : conventions, écart assumé vs cwl-assets (package installé, pas de
   scripts rsyncés), périmètre (⛔ **PAS d'enregistrement server, PAS de `pid:Milestone`** —
   retiré du périmètre en revue v2, c'est du scope creep), la justification du `python` en
   dur (décision n°1), le choix de `successCodes` (voir ci-dessus), et les commandes de
   validation et de run local.
6. **Oracle O2 (local, hors gate)** : tente-le sur **1 site / 10 jours, fenêtre PASSÉE ET
   CLOSE (`--to` ≤ J−7)**, deux `TINY_WAE_DATA_ROOT` **vierges** (un pour le run CWL, un pour
   le run CLI direct), et compare les ensembles `(item_id, statut)`. ⚠ **Clause bornée de la
   fiche** : en cas de ≥ 1 `failed`, tu rejoues **une seule fois** ; un `failed` au second
   essai est un **ÉCHEC de l'oracle**, pas une invitation à rejouer. Si le réseau est
   indisponible, déclare O2 **non mesuré** — ⛔ ne le déclare jamais vert sans l'avoir exécuté.

## Définition de « terminé »

- [ ] 3 fichiers CWL validés + README + gate branché.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | `just cwl` | 0 erreur sur les 3 fichiers ; `just check` vert avec la recette intégrée |
| O2 [local, hors gate] | `cwltool workflow.cwl` sur 1 site / 10 jours, `TINY_WAE_DATA_ROOT` VIERGE, fenêtre PASSÉE ET CLOSE (`--to` ≤ J−7) vs run CLI direct sur un second data_root vierge | ensembles `(item_id, statut)` identiques. ⚠ **Clause bornée** : si l'un des deux runs contient ≥ 1 `failed`, rejouer **une seule fois** ; un `failed` au second essai est un **ÉCHEC de l'oracle**, pas une invitation à rejouer indéfiniment |
| O3 | STDOUT du step search | JSON parsable seul (test) |

**Non testé** : tool update (l0-06.2) ; worker PID-FLOW (hors lot).

---
## Résumé de réalisation
*(à remplir avant `fait/`)*
