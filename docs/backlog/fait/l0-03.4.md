---
id: l0-03.4
titre: CLI ingest — 2 formes d'appel, idempotence grid_hash, --force, retry, SCL-first
effort: M
categorie: pipeline
phase: O1
depends_on: [l0-03.2, l0-03.3]
parent: l0-03
---

# [l0-03.4] — CLI ingest

## Objectif

Assembler : recherche (ou liste fournie) → pour chaque item : garde → SCL → verdict →
chips → manifeste. Relançable à l'infini.

## Périmètre (fichiers nommés)

- `src/tiny_wae/cli/ingest.py` (auto-découvert) : `ingest --acquisitions <envelope.json>`
  (chaînage CWL) ET `ingest --site A01 --from … --to …` (appelle StacSource). ⚠ **PAS
  d'option `--now`** (décision E-d : les deux formes portent une fenêtre explicite, l'option
  serait morte — `--now` vit sur `backfill` et `update`). Compteurs finaux sur STDERR (**found_stac /
  skipped_scene_cloud / off_tile / found_tile** puis ingested/rejected_*/skipped/failed +
  bytes + durée) ; `run.json` écrit via manifests API (les 4 compteurs de l'enveloppe y
  sont recopiés — cf. schéma l0-03.2, avec ses deux invariants de conservation).
- **Ordre invariant** (chapeau) : garde epsg → SCL seul → verdict → bandes 10 puis 20 m →
  ⭐ **garde nodata finale** (`chip_nodata_pct > chip_nodata_pct_max` → statut
  `rejected_nodata`, fichiers écrits **supprimés**, manifeste conservé — arbitrage n°3) →
  manifeste (dernier, atomique). Un rejet écrit un manifeste SANS chips.
- **Idempotence** : manifeste existant pour item_id **ET `grid_hash` identique** → skip
  compté. `grid_hash` différent (coordonnées corrigées) → ré-ingestion. `--force` →
  ré-ingestion inconditionnelle.
- **Retry/backoff au niveau requête** (paramètres `http_retries`/`http_backoff_s` de
  settings) sur STAC et lectures COG ; échec définitif → manifeste `failed` + cause,
  JAMAIS d'exception avalée. Un item en échec n'arrête pas les suivants. **Codes de
  sortie** (règle du chapeau l0-01) : `OK=0` · `FAILURE=1` si ≥ 1 failed avec au moins un
  succès · ⭐ **`INCONCLUSIVE=3` si TOUS les échecs sont d'origine réseau et aucun item
  n'a abouti** (amont injoignable ≠ bug — obligatoire aussi dans `backfill` et `update`).

- ⭐ **Smoke MINIMAL (ajout PO du 21/08 — le gate était creux)** : `scripts/smoke.py`
  cesse d'être un placeholder ici, pas six niveaux plus loin. Il exécute `ingest` de bout
  en bout sur un **double en mémoire du port `StacSource`** + un raster synthétique écrit
  par le script (aucun réseau, aucune fixture externe), et asserte : 3 fichiers produits,
  manifeste conforme, `assets_read > 0`. Le smoke **complet** (corpus réel, 2 modes,
  garde de contrat) viendra en l0-03.7 et le remplacera.
  *Motif : sans cela, dix fiches se valideraient sur un `just check` dont le volet smoke
  passe vert sans rien exécuter — conforme à la lettre de la règle « le gate appartient à
  la fiche qui le change », contraire à son esprit.*

## ⚠ Ancrage dans le code réel (22/08/2026, avant dispatch — run N4)

Vérifié sur `develop` @ `4e20384`, **après** le merge de l0-03.3 et l0-02.2 — faits, pas
suppositions. Les faits marqués « à reconfirmer post-merge » pendant le run N3 ont TOUS été
reconfirmés sur le code mergé (signatures relevées ci-dessous).

### L'existant à consommer tel quel

- **`adapters/chips.py` (l0-03.3, mergé)** — contrat exact :
  ```python
  NODATA_VALUE = 0 · OFFLINE_ENV_VAR = "TINY_WAE_OFFLINE"
  CHIP_10M_FILENAME = "chip.tif" · CHIP_20M_FILENAME = "chip_20m.tif" · SCL_FILENAME = "scl.tif"
  class EpsgMismatchError(ValueError) · class RemoteAccessForbidden(ValueError)
  class ChipResult: files · assets_read · content_hashes · bytes_written · chip_nodata_pct
  check_epsg(acq, grid) -> None
  read_scl(acq, grid, settings)       -> tuple[np.ndarray, int]
  read_bands_10m(acq, grid, settings) -> tuple[np.ndarray, int]
  read_bands_20m(acq, grid, settings) -> tuple[np.ndarray, int]
  write_chips(dest_dir, *, bands_10m, bands_20m, scl, grid, item_id, assets_read) -> ChipResult
  ```
  Les `read_*` rendent `(array, nombre d'ouvertures)` — **c'est toi qui sommes** les trois
  compteurs pour obtenir l'`assets_read` de l'item. Arrays **band-major** `(bands, H, W)`.
  ⭐ `chip_nodata_pct` est déjà en **pourcentage 0–100** ; `write_chips` le calcule mais ne
  décide RIEN : la comparaison à `settings.chip_nodata_pct_max`, le statut `rejected_nodata`
  et la **suppression des 3 fichiers** sont à faire ICI.
- **`adapters/manifests.py` (l0-03.2)** — API complète, ⛔ ne rien y réécrire :
  `write_manifest(data_root, manifest) -> Path` (atomique, **à appeler EN DERNIER**),
  `read_manifest(data_root, site_id, item_id) -> Manifest` (`FileNotFoundError` si absent),
  `list_for_site`, `item_ids_for_site(data_root, site_id) -> set[str]`, `last_datetime`,
  `write_run(data_root, run) -> Path` (valide les **deux** invariants avant d'écrire, lève
  `ConservationError` sans rien écrire), `read_run`, `list_runs`, `aggregate_counters`, et
  `grid_hash(grid, settings, *, band_order_10m=…, band_order_20m=…) -> str`.
  Chemins figés : `<data_root>/{site_id}/{item_id}/manifest.json` et
  `<data_root>/{site_id}/runs/{run_id}.json`.
  ⚠ `read_manifest` fait `Manifest(**data)` : le manifeste écrit porte **exactement** les
  24 champs du dataclass — ni plus, ni moins.
- **`Manifest`** : `schema_version, site_id, item_id, datetime, tile, sequence, platform,
  status, cause, invalid_pct, cloud_pct, chip_nodata_pct, scl_class_counts,
  processing_baseline, boa_offset_applied, radiometry, grid_hash, assets_read,
  content_hashes, bytes_downloaded, bytes_written, duration_s, files, versions`.
  ⚠ **`scl_class_counts` y est un `dict[str, int]` (clés CHAÎNES — c'est du JSON)** alors que
  `core.scl.verdict` attend un `dict[int, int]`. La conversion est à ta charge, dans les deux
  sens. Un test doit la figer, sinon la relecture d'un manifeste rendra des clés muettes.
  ⚠ `radiometry` au manifeste est `dict[str, dict[str, float] | None]` (`{"scale":…, "offset":…}`),
  alors que `Acquisition.radiometry` est `dict[str, tuple[float,float] | None]` — **conversion
  à faire aussi**.
- **`Run`** : `schema_version, site_id, run_id, window, counters, assets_read,
  bytes_downloaded, tile_suspect, duration_s`. ⭐ **`tile_suspect` existe déjà** — l'oracle
  O5ter n'a AUCUN champ à ajouter au schéma.
- **`MANIFEST_STATUSES`** (5 valeurs, `skipped` jamais écrit à un manifeste) et
  **`RUN_STATUSES`** (les 6 statuts normatifs) : les **importer**, jamais les recopier.
- **`core/scl.verdict(scl_class_counts: dict[int,int], settings) -> Verdict`** →
  `Verdict(status, invalid_pct, cloud_pct)`, `status ∈ {ingested, rejected_invalid,
  rejected_clouds}`. Ordre des seuils déjà appliqué, dénominateur = somme de TOUTES les
  classes, lève `ValueError` si la somme est nulle. ⭐ Il ne connaît PAS `rejected_nodata` :
  la garde nodata est **postérieure** (ordre invariant du chapeau).
- **`adapters/stac.py`** : `StacSource` (Protocol, `search(site, window) -> Envelope`),
  `EarthSearchSource(settings)`, **`StacUnreachable(Exception)`** (l0-02.2 — c'est l'exception
  qui porte l'exit 3 de l'oracle O6), `StacSourceError(ValueError)`.
- **`core/envelope.Envelope`** : `schema_version, site_id, window, counters, items` +
  `to_dict()` / `from_dict(data)`, invariants vérifiés **à la construction**.
- **`cli/exit_codes.py`** : `OK=0 · FAILURE=1 · USAGE=2 · INCONCLUSIVE=3`.
- **Auto-découverte** : déposer `cli/ingest.py` avec `register(app)` ; ⛔ `__main__.py` figé.
  Modèle de wiring à copier : `cli/search.py` (l0-02.2).
- **`scripts/smoke.py` est encore le placeholder de scaffold** (imprime une ligne, sort 0).
  C'est bien cette fiche qui le remplace. ⚠ **`mypy` ne couvre PAS `scripts/`**
  (`files = ["src"]` dans `pyproject.toml`) — d'où la décision n°2 ci-dessous.
- **`tests/fixtures/manifests/C07/runs/run-2026-01-31.json`** montre le format de run_id
  déjà présent dans le dépôt (`run-2026-01-31`) : le schéma ne contraint PAS ce format.

### Décisions d'ancrage (prises ici — ne pas rouvrir, ne pas re-délibérer)

1. ⭐ **L'orchestration NE VIT PAS dans `cli/`** (règle de couche : `cli/` = wiring pur).
   Créer **`src/tiny_wae/adapters/ingestion.py`** qui porte toute la boucle (garde epsg →
   SCL → verdict → bandes → garde nodata → manifeste), et **`src/tiny_wae/cli/ingest.py`**
   qui ne fait que : parser les options, charger la config, appeler l'orchestrateur, écrire
   les compteurs sur STDERR, mapper les exceptions sur les codes de sortie.
   *Motif décisif : le smoke doit exécuter le pipeline réel en important l'orchestrateur.
   Si la boucle vit dans le CLI, le smoke doit passer par un sous-processus — et l'oracle O7
   (« test négatif : double du port qui ne rend rien → smoke ROUGE ») devient intestable.*
2. **La logique du smoke RESTE dans `scripts/smoke.py`** — le chapeau l0-01 gèle la taxonomie
   des sous-commandes à 9 et dit explicitement que `smoke` **n'est pas** un module `cli/`.
   Structure-le en fonctions (`build_fake_source()`, `run_smoke(dest) -> None`, `main()`), et
   fais que le test O7 l'importe par
   `importlib.util.spec_from_file_location("smoke", "scripts/smoke.py")`. Le script écrit ses
   sorties dans un `tempfile.TemporaryDirectory()`, **jamais dans `./data`**.
3. **`run_id`** : fonction interne `_new_run_id() -> str` dans `adapters/ingestion.py`, format
   `run-YYYYMMDDTHHMMSSZ` (UTC, à la seconde), **monkeypatchée par les tests**.
   ⛔ **Ne PAS ajouter d'option `--now` ni `--run-id`** : `--now` est explicitement banni de
   `ingest` par la décision E-d du chapeau, et une option de plus serait une décision de
   contrat non demandée. La date seule (format des fixtures) est **refusée** : deux runs le
   même jour écraseraient le même `run.json`, et l'oracle O2 fait justement deux runs.
4. **Point de couture du port** : `cli/ingest.py` expose **son propre**
   `build_source(settings) -> StacSource`. ⛔ Ne pas importer celui de `cli/search.py` (deux
   CLIs qui partagent un point de monkeypatch se contaminent en test).
5. ⭐ **Distinguer « grille non calculée » de « epsg qui ne correspond pas »** (défaut de
   diagnostic relevé à la revue de l0-03.3) : `check_epsg` lève `EpsgMismatchError` dans les
   DEUX cas. Avant la boucle, vérifie `site.grid.epsg is None` → message explicite pointant
   `just survey-tiles` et **exit `USAGE` (2)**, jamais un item `failed`. Une config
   incomplète n'est pas un item en échec.
6. **Retry/backoff** : `settings.http_retries` et `settings.http_backoff_s`, appliqués aux
   appels STAC ET aux lectures de rasters. Isole l'attente dans une fonction interne
   (`_sleep(seconds)`) pour que les tests la neutralisent — ⛔ **aucun test ne doit dormir**.
   Un item en échec définitif → manifeste `failed` + `cause` renseignée ; il **n'arrête
   jamais** les items suivants ; aucune exception n'est avalée en silence.
7. **`bytes_downloaded` = 0 pour les lectures de rasters** (GDAL n'est pas instrumentable
   depuis Python — mesuré en revue v3). C'est une donnée de rapport, **jamais un critère de
   gate ni d'oracle**. Ne l'invente pas.
8. **`tile_suspect`** (O5ter) : vrai quand `rejected_nodata / found_tile > 0,20` — le
   dénominateur est **`found_tile`** (les items réellement instruits), jamais `found_stac`
   (décision E-a du chapeau l0-02 : le mot `found` seul est banni). Le CLI **signale et
   s'arrête là** : ⛔ aucune auto-correction de tuile en cours de run.
9. **`versions`** au manifeste : `{"tiny_wae": tiny_wae.__version__}`.
10. **Codes de sortie** : `0` si aucun `failed` · `1` si ≥ 1 `failed` **et** au moins un item
    abouti · `3` si **tous** les échecs sont d'origine réseau **et** aucun item n'a abouti ·
    `2` pour toute erreur de config/usage (dont la grille non calculée, cf. n°5).

**Versions mesurées** : rasterio 1.5.1 · pystac-client 0.9.0 · numpy 2.5.2 · affine 3.0.0.
⛔ Aucune montée de version, `pyproject.toml` non touché.

## Définition de « terminé »

- [ ] CLI 2 formes + idempotence + retry, tests sur doubles en mémoire du port
      `StacSource` (les fixtures COG réelles arrivent en l0-03.5).
- [ ] `scripts/smoke.py` remplacé par le smoke minimal ci-dessus ; `just check` l'exécute
      réellement.

## Oracle / recette (sur fixtures — le live vit en l0-03.7)

| # | Critère | Seuil |
|---|---|---|
| O1 | Run sur fixture N items (mix clair/nuageux/invalide) | manifestes conformes au verdict attendu de chaque item ; **les deux invariants de conservation bouclent** dans le `run.json` produit ; compteurs == valeurs gelées dans le test (pas recopiées de l'enveloppe à l'exécution) |
| O2 | **Run double** (décision E-d — témoins positif ET négatif) | **1er run : `assets_read > 0`** (le compteur fonctionne) ; **2e run : `assets_read == 0`, `skipped == N`, `content_hashes` identiques** (hash du tableau décodé + CRS/transform/dtype, jamais des octets du GeoTIFF). `bytes_downloaded` n'est PAS un critère ici |
| O3 | `grid_hash` changé (grille mutée dans la config du test) | l'item est RÉ-ingéré, nouveau transform correct |
| O4 | Item `sequence=1` même date que le `_0` | 2 répertoires distincts, zéro écrasement |
| O5 | Échec injecté (double du port lève) sur 1 item / N | manifeste `failed`+cause, les N-1 autres traités, exit 1 |
| O5bis | **Garde nodata** (arbitrage n°3) : fixture à 40 % de nodata sur le chip | statut `rejected_nodata`, `chip_nodata_pct` au manifeste, **aucun fichier laissé sur disque** ; à 0,5 % → `ingested` |
| O5ter | ⭐ **Règle de repli tuile** (correction PO v2 n°31, jamais écrite avant) : un site dont **> 20 % des items de sa `reference_tile` sont `rejected_nodata`** | le CLI **signale** le site en fin de run (STDERR + champ `tile_suspect: true` dans `run.json`) et **s'arrête là** : la bascule de tuile est une **décision de config** (édition de `sites.yaml` + `just survey-tiles`), jamais une auto-correction en cours de run |
| O6 | Amont injoignable simulé (le double du port lève une erreur réseau sur tous les items) | **exit 3** (`INCONCLUSIVE`), distinct du cas O5 qui rend 1 |
| O7 | **Smoke minimal** dans `just check` | s'exécute RÉELLEMENT (assertion : le script produit 3 fichiers et un manifeste) ; test négatif : double du port qui ne rend rien → smoke ROUGE |
| O8 | `just check` | vert, hors ligne |

**Non testé** : réseau réel (l0-03.7 --live) ; parallélisme (l0-04.1).

---
## Résumé de réalisation

**Livré** (commit `bd96c1f`, mergé en `f7c8a4e`, corrigé par `00f3eb1`) :
`adapters/ingestion.py` (489 l., toute l'orchestration), `cli/ingest.py` (203 l., wiring
pur), `scripts/smoke.py` (le placeholder est mort), `tests/test_ingestion.py`,
`tests/test_cli_ingest.py`, `tests/test_smoke.py`. **122 → 146 tests** (+21 par l'agent,
+3 à la revue). `just check` vert sur `develop` : mypy strict **25 fichiers**, **146
passed**, et ⭐ **le smoke exécute désormais le pipeline réel** :
`smoke: vert — ingest bout en bout (double en mémoire, 1 item, 3 fichiers, assets_read > 0)`.
**Le gate n'est plus creux** — c'était l'enjeu de l'ajout PO du 21/08.

### Verdict d'oracle chiffré

| # | Seuil | Mesuré | Verdict |
|---|---|---|---|
| O1 | statuts conformes, invariants bouclés, compteurs gelés | 5 items ; `found_stac=7 · skipped_scene_cloud=1 · off_tile=1 · found_tile=5 · ingested=1 · rejected_clouds=1 · rejected_invalid=1 · rejected_nodata=1 · failed=1` **en littéral** (7 = 1+1+5 ✓ ; 5 = 1+1+1+1+1 ✓) | ✅ |
| O2 | témoins positif ET négatif | 1er run `assets_read > 0` ; 2e run `assets_read == 0`, `skipped == 2`, `content_hashes` identiques | ✅ |
| O3 | grille mutée → ré-ingestion | origine +60 m → `skipped=0`, `grid_hash` différent, transform de sortie == nouvelle grille | ✅ |
| O4 | `sequence=1` même date | 2 répertoires distincts, zéro écrasement | ✅ |
| O5 | 1 échec / N | `failed=1` + `cause` non vide, 2 autres `ingested`, exit **1** | ✅ |
| O5bis | garde nodata dans les deux sens | 37,5 % → `rejected_nodata`, `chip_nodata_pct` au manifeste, **0 fichier sur disque** ; 0,5 % → `ingested` | ✅ |
| O5ter | `tile_suspect` au-delà de 20 % | 2/5 = 40 % → `true` ; 1/5 = 20 % pile → `false` (seuil **strict**) | ✅ |
| O6 | amont injoignable → exit 3 | tous les items en échec réseau → exit **3**, distinct du **1** de O5 ; `StacUnreachable` sur `search()` → exit 3 sans qu'aucun item soit tenté | ✅ |
| O7 | smoke réel dans le gate | positif (3 fichiers + manifeste `ingested` + `assets_read > 0`) **et** négatif (source vide → `AssertionError`, gate rouge) | ✅ |
| O8 | `just check` vert hors ligne | vert (cf. ci-dessus) | ✅ |

### Contrat exposé (consommé par l0-03.5, l0-04.1, l0-05.2)

```python
@dataclass(frozen=True, slots=True)
class IngestOutcome:
    run: Run
    all_failures_network: bool

def ingest_from_envelope(*, envelope, grid, settings, data_root, force=False) -> IngestOutcome
def ingest_from_source(*, site, window, source, settings, data_root, force=False) -> IngestOutcome
```
Internes monkeypatchées par les tests (pas un contrat public) : `_new_run_id()`, `_sleep()`,
`_retry_call()`, `_is_network_error()`.

### ⭐ Deux corrections apportées à la revue avant clôture (commit `00f3eb1`)

1. **`_retry_call` retentait TOUTE exception**, y compris déterministe (clé d'asset absente
   de l'item). Une telle erreur ne se résout jamais : la retenter coûte
   `http_retries × http_backoff_s` = **6 s par item**, à multiplier par les 25 sites × 48
   mois de l0-04.H. Le retry est désormais réservé aux erreurs d'origine réseau.
   *L'agent avait lui-même signalé ce choix dans ses décisions hors fiche — signalement utile.*
2. **`_is_network_error` ne reconnaissait que `OSError`**, donc classait `StacUnreachable`
   comme « pas réseau ». Sans effet visible aujourd'hui (elle ne survient que sur `search()`,
   hors boucle), mais c'était un piège posé pour **l0-04.1 et l0-05.2**, qui réutiliseront cet
   orchestrateur avec d'autres sources. `StacUnreachable` n'hérite pas d'`OSError`
   volontairement (décision l0-02.2) : il fallait donc la nommer explicitement.

**+3 tests** dont le **témoin positif du retry**, que l'agent avait listé comme non testé :
seul le chemin « échoue toujours » était couvert (O5/O6), donc **rien ne prouvait que
`http_retries` serve à quelque chose** — un retry qui ne retenterait jamais aurait laissé
O5 et O6 verts.

### Décisions prises en cours de réalisation

1. **Granularité des fixtures de test** : une grille 4×4 ne permet pas d'exprimer 40 % ni
   0,5 % exactement (6,25 %/pixel) → 37,5 % (6/16) pour le rejet, grille dédiée 20×20 pour
   le cas 0,5 % (2/400). Aucun impact sur le code de production.
2. **Classification réseau** : `isinstance(exc, OSError)` — couvre le transport HTTP et
   `rasterio.errors.RasterioIOError` (qui en hérite), ce qui permet de simuler O5 et O6 sans
   réseau réel, par des hrefs vers des fichiers inexistants. **Étendue à `StacUnreachable`
   à la revue.**
3. **Manifeste `rejected_nodata`** : `files=[]` et `content_hashes={}` après suppression —
   reflète l'état réel du disque plutôt qu'un contenu calculé puis effacé.

### Non testé (explicite)

Réseau réel et fixtures COG réelles (l0-03.5 pour le corpus, l0-03.7 pour `--live`) ·
`bytes_downloaded` jamais vérifié à une valeur non nulle (toujours 0 — non instrumentable
côté GDAL, donnée de rapport et non critère de gate) · **parallélisme** : la boucle est
strictement séquentielle et n'a jamais tourné sous concurrence (l0-04.1) · les sous-cas de
JSON `--acquisitions` gravement malformé au-delà de ceux que `Envelope.from_dict` couvre déjà.
