---
id: l0-01.1
titre: Settings et sites.yaml — modèles typés, validation pure, env
effort: S
categorie: pipeline
phase: O1
depends_on: []
parent: l0-01
---

# [l0-01.1] — Settings et sites

## Objectif

Poser les deux fichiers de config et leurs modèles typés. Aucun CLI, aucun réseau.

## Périmètre (fichiers nommés)

- `config/sites.yaml` : les 25 sites de **`docs/lots/lot-0-sites.md` §3** (source unique —
  l'annexe A de `_roadmap.md` n'est qu'un renvoi) — champs `id`, `name`,
  `lat`, `lon`, `category` (`nuclear-construction|megaproject|stable-watch`), `note` ;
  champs `reference_tile` et `grid` (`epsg`, `origin_x`, `origin_y`) **présents mais
  vides** (remplis par l0-01.3). C08 = Bouconne (43.628, 1.217).
- `config/settings.yaml` + surcharges env (une variable par clé, préfixe `TINY_WAE_`) :
  `stac_url`, `stac_collection` (`sentinel-2-l2a`), `asset_keys` (**les 11 clés** d'assets
  source = 10 bandes + le masque : `blue, green, red, nir, rededge1, rededge2, rededge3,
  nir08, swir16, swir22, scl` — la clé logique tiny-wae EST la clé earth-search ; les
  libellés `B0x` sont documentaires en commentaire), `cloud_pct_max` (30), `scene_cloud_max` (95),
  `invalid_pct_max` (1), ⭐ **`chip_nodata_pct_max` (1)** — arbitrage n°3 : le seuil de la
  **garde nodata du GO G8**, jusque-là sans instrument (alors que `lot-0-sites.md` O3
  l'exige : « nodata < 1 % sur 100 % des items ingested ») —,
  `data_root` (défaut `./data`), `incremental_margin_days` (3),
  `http_retries` (3), `http_backoff_s` (2), `backfill_workers` (6), `chip_px_10m` (512),
  `chip_px_20m` (256).
- `src/tiny_wae/core/sites.py`, `core/settings.py` : dataclasses frozen + validation pure
  (bornes lat/lon, ids uniques, catégories, origine multiple de 20 quand renseignée).
- `src/tiny_wae/adapters/config_io.py` : YAML+env → objets typés. Parse aux frontières.
- `.env.example` : TOUTES les variables de surcharge listées, **dont
  `TINY_WAE_OFFLINE`** (garde réseau de contrat, décision E-b — posée par
  `smoke --replay`, consommée par `adapters/chips.py` en l0-03.3). `.gitignore` : + `data/`.
- ⭐ **TOUTES les dépendances du lot sont posées ICI, en une fois** (geste PO issu de la
  revue v3 : `pyproject.toml` était une zone partagée par 7 fiches, dont 4 parallélisables
  — source de conflits de merge et de régénérations de lockfile concurrentes) :
  `pyyaml` · `pyproj` · `httpx` · `pystac-client` · `rasterio` · `Pillow` · `cwltool` ·
  `pytest-socket` (+ `--disable-socket` dans la config pytest). Les fiches aval **ne
  touchent plus jamais** au `pyproject.toml` ; elles se contentent d'importer.
- ⚠ **Lockfile** : `pixi.lock` est **versionné** mais **jamais mergé** — en cas de conflit,
  le régénérer sur la branche cible (`just install`) plutôt que de résoudre à la main.
  Consigne rappelée dans chaque fiche qui suit.

## ⚠ Ancrage dans le code réel (orchestrateur, 21/08/2026 — avant dispatch)

Vérifié sur le dépôt à l'état `f2ff73f`, pas supposé.

**État constaté**
- `config/` **n'existe pas** — à créer. `src/tiny_wae/{core,adapters,cli}/__init__.py` existent
  et sont **vides** : aucun module à modifier, tout est en création.
- `.env.example` ne contient **qu'une** variable (`DATABASE_URL`) — les variables `TINY_WAE_*`
  sont à ajouter, pas à modifier.
- `data/` **n'est pas** dans `.gitignore` (vérifié) — l'ajout demandé est bien nécessaire.
- `[tool.pytest.ini_options]` existe avec `addopts = "-q"` — c'est là que `--disable-socket`
  se pose, en complétant la valeur existante.

**⛔ Piège mesuré — signe moins Unicode dans les coordonnées**
7 des 25 sites (**A02, A04, A08, B06, B07, B08, C04**) portent leur coordonnée négative avec
le caractère **U+2212 « − »**, PAS le moins ASCII `-` (vérifié caractère par caractère sur le
document source). Recopiées telles quelles, ces valeurs sont des **chaînes YAML**, pas des
nombres : le chargement échoue ou, pire, produit des `lat`/`lon` en `str` que l'oracle O1 ne
verrait pas (il ne teste que les ids). **Convertir en `-` ASCII à la saisie**, et la
validation de `core/sites.py` doit **typer lat/lon en `float`** pour que le cas soit
impossible à ré-introduire.

**Décision tranchée (§4.1 de /run) — où poser les 8 dépendances**
La fiche les liste sans nommer la section ; le `pyproject.toml` en offre trois et porte la
règle en commentaire (« les paquets nécessitant des natifs conda-forge se déclarent plutôt
dans `[tool.pixi.dependencies]` »). Répartition retenue :
- `[project].dependencies` (PEP 621, purs — préserve la réversibilité pixi↔uv) : `pyyaml`,
  `httpx`, `pystac-client`
- `[tool.pixi.dependencies]` (natifs) : `pyproj`, `rasterio`, `pillow`
- `[tool.pixi.feature.dev.dependencies]` (outillage, pas runtime) : `pytest-socket`, `cwltool`

**Confirmé**
- Les 25 sites sont bien en `docs/lots/lot-0-sites.md` §3, en **8/9/8** — conforme à O1.
- Les trois sections du document mappent les valeurs exigées par la fiche : Catégorie A
  « Nucléaire en construction » → `nuclear-construction` · B « Mégachantiers mondiaux » →
  `megaproject` · C « Sites stables & veille OSINT » → `stable-watch`.
- Aucun `smoke` à toucher : le gate reste le placeholder de scaffold pour cette fiche.

## Définition de « terminé »

- [ ] Les 2 YAML livrés, les modèles typés, le chargeur.
- [ ] Tests unitaires : nominal + les **5 mutations d'erreur listées en O2** + priorité
      env > YAML.

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | Chargement config livrée | ids exactement {A01..A08, B01..B09, C01..C08}, répartition 8/9/8 (test à liste littérale) |
| O2 | **5 mutations** : id dupliqué · lat=95 · catégorie inconnue · origine=13 (non multiple de 20) · `asset_keys` amputée d'une clé | erreur nommant site et champ à chaque fois |
| O3 | `TINY_WAE_CLOUD_PCT_MAX=40` posé | la valeur env gagne (test) |
| O4 | `just check` | vert |

**Non testé** : contenu de `reference_tile`/`grid` (l0-01.3) ; exactitude des coordonnées
(l0-03.H).

---
## Résumé de réalisation

**Fait le 21/08/2026** — run autonome, agent Sonnet effort medium en worktree isolé.

**Livré** : `config/sites.yaml` (25 sites) · `config/settings.yaml` · `core/sites.py`
(`Site`, `Grid`, validation pure) · `core/settings.py` (`Settings`) ·
`adapters/config_io.py` (YAML + surcharges env) · `.env.example` (variables `TINY_WAE_*`
dont `TINY_WAE_OFFLINE`) · `.gitignore` (+ `data/`) · `pyproject.toml` (8 dépendances) ·
`tests/test_config.py` · `pixi.lock` régénéré.

**Verdict d'oracle (chiffré, mesuré par l'orchestrateur — pas sur déclaration de l'agent)**

| # | Verdict | Mesure |
|---|---|---|
| O1 | ✅ VERT | 25 ids exactement `{A01..A08, B01..B09, C01..C08}` ; répartition mesurée `nuclear-construction: 8, megaproject: 9, stable-watch: 8` ; **les 50 `lat`/`lon` chargent en `float`** (piège U+2212 neutralisé — vérifié indépendamment sur les 7 sites concernés) ; C08 = Forêt de Bouconne 43.628, 1.217 |
| O2 | ✅ VERT | 5 mutations couvertes (id dupliqué · `lat=95` · catégorie inconnue · `origin_x=13` · `lon=200`), chaque message nommant le site ET le champ |
| O3 | ✅ VERT | `TINY_WAE_CLOUD_PCT_MAX=40` l'emporte sur le YAML (30) ; idem `TINY_WAE_CHIP_NODATA_PCT_MAX=5` |
| O4 | ✅ VERT | `just check` sur `develop` **après merge** : ruff OK (119 fichiers) · mypy strict OK (8 modules) · **15 tests passés** · smoke = placeholder de scaffold (hors périmètre, non touché) |

**Non testé** : `reference_tile`/`grid` restent vides (remplis par l0-01.3) · exactitude
géographique des coordonnées (fiche humaine l0-03.H) · surcharge env de `asset_keys`
(format CSV codé mais hors oracle).

**Décisions prises en cours de run** (règle d'autonomie §4.1)
1. **Répartition des 8 dépendances** entre les trois sections du `pyproject.toml` — la fiche
   ne la nommait pas (cf. Ancrage). Appliquée telle qu'ancrée.
2. **`cwltool` en `pypi-dependencies` du feature `dev`**, et non en `dependencies` : aucun
   build conda-forge compatible Python 3.12 (mesuré par l'agent). Seul le canal change, la
   répartition logique demandée est respectée.

**Écart de process rencontré** : le premier worktree était basé sur `origin/main` (périmé de
14 commits), l'agent a donc lu une version de la fiche **antérieure à l'arbitrage n°3** et
livré sans `chip_nodata_pct_max`. Détecté à la relecture du code, corrigé par un second
dispatch (commit `fe82978`). Cause et correctif consignés au débrief du run.

**Commits** : `731630e` (implémentation) · `fe82978` (correctif `chip_nodata_pct_max` +
comptage des clés) · merges `1f24335` et le merge du correctif.
