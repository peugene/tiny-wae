---
id: l0-01.3
titre: Relevé des tuiles de référence (règle D-c, marge géométrique) + calcul des grilles
effort: M
categorie: pipeline
phase: O1
depends_on: [l0-01.2]   # sites-validate + exit_codes ; l0-01.1 arrive par transitivité
parent: l0-01
---

# [l0-01.3] — Tuiles de référence et grilles

## Objectif

Remplir `reference_tile` et `grid` pour les 25 sites. ⚠ Fiche à **réseau réel** (25
requêtes /aggregate) — exécutée par l'agent, résultats = DONNÉES commises dans
`sites.yaml`.

## Périmètre (fichiers nommés)

- `scripts/survey_tiles.py` (rejouable, hors package) — ⭐ **CLI avec l'option
  `--sites A01,C07` (défaut : tous)**, indispensable à la re-passe ciblée que la fiche
  humaine l0-03.H prescrit après correction de coordonnées. **Recette dédiée
  `just survey-tiles [sites]`** dans le justfile (règle façade : jamais `python …` à la
  main — sinon l'environnement pixi n'est pas actif et `pyproj`/`httpx` manquent).
- Pour chaque site : bbox du chip (5,12 km centré) →
  `GET {stac_url}/aggregate?collections={collection}&bbox=…&datetime=<48 mois>&aggregations=grid_code_frequency`
  pour lister les tuiles candidates. Multi-tuiles = cas FRÉQUENT (mesuré : A03, A04, C07
  sur 7 sondés).
- ⭐ **Source de l'emprise d'une tuile (le point que la v3 disait manquant)** : `/aggregate`
  ne rend que des codes MGRS. Pour chaque tuile candidate, faire **un `/search?limit=1`
  filtré sur ce `grid:code`** et lire `proj:transform` + `proj:shape` de l'asset `blue`
  → origine (ULX/ULY) et emprise (109 800 m de côté à 10 m). ~2 requêtes par site.
  *(Les origines suivent la règle « coin MGRS 100 km calé sur le réseau 60 m » — vérifié
  sur 4 tuiles — mais on la LIT plutôt que de la recalculer : une mesure vaut mieux
  qu'une dérivation.)*
- **Règle D-c (décision Philippe 21/08)** : `reference_tile` = tuile offrant la **marge
  géométrique maximale** entre le chip et le bord de l'emprise nominale de la tuile
  (calcul en UTM) ; en cas d'égalité, la plus fournie en items. Exemple attendu : C07 →
  52TEL (marge 4 160 m), PAS 52TDL (500 m).
- `grid` : `epsg` = zone UTM de la tuile de référence (326xx/327xx depuis le code MGRS) ;
  `origin_x/origin_y` = coin haut-gauche du chip 512×512@10 m centré sur (lat, lon),
  arrondi au multiple de 20 m inférieur. Formule écrite en docstring.
- Dépendances déclarées : `pyproj` (conversion lat/lon→UTM), `httpx` (requêtes).
- Le script écrit un rapport (`scripts/survey_tiles_report.json` : par site, buckets
  bruts, tuile choisie, marge calculée) — trace de la donnée, versionnée.

## ⚠ Ancrage dans le code réel (vérifié le 21/08/2026, base dafc58e)

**Vérifié dans le code, pas supposé :**

- ⛔ **`core/geometry.chip_bounds(grid, settings)` n'est PAS réutilisable ici** : il lève
  `GridNotComputedError` quand `grid.epsg/origin_x/origin_y` sont à `None` — c'est-à-dire
  exactement l'état des 25 sites que cette fiche doit remplir. Le calcul « (lat, lon) → bbox du
  chip en UTM » est une **fonction distincte**, à écrire ici.
- ⛔ **`adapters/config_io.py` est en LECTURE SEULE** : il n'a aucune fonction d'écriture
  (`load_settings`, `load_sites`, helpers privés). Rien à réutiliser pour écrire.
- ⚠⚠ **`config/sites.yaml` contient 11 lignes de commentaires**, dont un avertissement
  **critique** : les coordonnées négatives du document source utilisent le signe moins Unicode
  U+2212, recopiées ici en ASCII strict pour que YAML les parse en `float`. **Un
  `yaml.safe_dump` effacerait cet avertissement** — et le prochain qui éditera le fichier
  pourrait réintroduire le bug (7 sites concernés, détecté lors du run N0).
- `cli/exit_codes.py` (l0-01.2) fournit `OK/FAILURE/USAGE/INCONCLUSIVE` = 0/1/2/3 — O3 les
  importe, ne pas les redéfinir.
- `pyproj` **3.7.2**, `httpx` **0.28.1** installés (mesuré) — rien à ajouter au `pyproject.toml`.
- `pytest` tourne avec `addopts = "-q --disable-socket"` (mesuré dans `pyproject.toml`) :
  **aucun test ne peut faire de réseau**, ce qui est cohérent avec O4 (« le test réseau réel
  n'est PAS dans le gate »). Le relevé réel se lance à la main, hors `just check`.

**Décisions prises à l'ancrage :**

1. **Le module pur est `src/tiny_wae/core/tiles.py`** (la fiche dit « fonctions PURES dans
   `core/` » sans nommer le fichier) : conversion (lat, lon) → bbox UTM du chip, EPSG depuis le
   code MGRS, marge géométrique chip↔bord de tuile, arrondi de l'origine au multiple de 20 m
   inférieur. C'est ce module que testent O2 et O2bis, **sans réseau**.
2. **`sites.yaml` s'édite en préservant intégralement ses commentaires** : édition textuelle
   ciblée des champs `reference_tile:` et du bloc `grid:` de chaque site — **pas** de
   `yaml.safe_dump` du document entier (voir ci-dessus). Interdit d'ajouter une fonction
   d'écriture à `adapters/config_io.py` : hors périmètre de la fiche, et module de l0-01.1.
3. **Écriture atomique et sous condition** : le fichier n'est réécrit qu'après une relecture
   réussie (`load_sites` + `validate_sites` sur le contenu produit), via tmp + rename. Toute
   erreur — réseau injoignable compris — laisse `sites.yaml` **strictement inchangé** (O3).
4. **Cette fiche est la SEULE de la vague à toucher le `justfile`** : y ajouter `survey-tiles`
   et **ne pas supprimer ni modifier la recette générique `script`** qui vient d'y être posée
   (elle sert à une autre fiche dispatchée en parallèle).

**Zone d'écriture EXCLUSIVE** — 3 agents travaillent en parallèle sur N2 :
`scripts/survey_tiles.py`, `scripts/survey_tiles_report.json`, `src/tiny_wae/core/tiles.py`,
`tests/test_tiles.py`, `config/sites.yaml`, et l'ajout de la recette `survey-tiles` au `justfile`.
⛔ Ne toucher NI `core/acquisition.py|envelope.py`, NI `adapters/stac.py` (l0-02.1, en
parallèle), NI `core/windows.py` (l0-05.1, en parallèle), NI `pyproject.toml`, `pixi.lock`,
`config/settings.yaml`, `adapters/config_io.py`, `core/geometry.py`.

## Définition de « terminé »

- [ ] `sites.yaml` complété pour les 25 sites, rapport commité.
- [ ] Logique de choix (marge géométrique, arrondi 20 m, epsg depuis MGRS) en fonctions
      PURES dans `core/` testées à sec sur cas connus (C07 : 52TEL ; A01 : 31TGJ
      mono-tuile).
- [ ] `just survey-tiles` en place ; `--sites` fonctionnel (re-passe ciblée).

## Oracle / recette

| # | Critère | Seuil |
|---|---|---|
| O1 | `sites-validate` après remplissage | exit 0 ; 25/25 avec `reference_tile` non vide, `grid.epsg` == zone déduite du code MGRS, origines multiples de 20 |
| O2 | Cas C07, **test PUR** sur des entrées littérales : bbox du chip + emprises des deux tuiles (52TDL ULX 399960, 52TEL ULX 499980, côté 109 800 m) | choix = **52TEL** (marges calculées ≈ 495 m vs 4 155 m). ⚠ L'oracle porte sur des **géométries**, pas sur les comptes d'items — un décompte ne contient aucune marge (défaut de la v3 corrigé) |
| O2bis | Départage : deux tuiles de marges égales, comptes 319 vs 317 | choix = la plus fournie (règle secondaire, testée séparément) |
| O3 | Réseau injoignable | exit 3 (`INCONCLUSIVE`), sites.yaml non modifié |
| O4 | `just check` | vert (le test réseau réel n'est PAS dans le gate) |

**Non testé** : évolution future du catalogue (le script est rejouable) ; couverture
réelle des chips (l0-03).

---
## Résumé de réalisation

**Fait** (agent Sonnet medium, worktree isolé) : relevé réseau réel des 25 tuiles de référence,
calcul des grilles, et remplissage de `config/sites.yaml`.

Livré : `core/tiles.py` (logique pure), `scripts/survey_tiles.py`, `scripts/survey_tiles_report.json`
(trace de la donnée), recette `just survey-tiles`, **13 tests**. `config/sites.yaml` rempli.
Total du projet après merge : **82 tests**, mypy strict sur 18 modules.

**Réseau réel** : ~52 requêtes vers earth-search (25 sites × `/aggregate` + `/search`, plus les
candidates supplémentaires des sites multi-tuiles), toutes en 200.

| # | Verdict | Mesure |
|---|---|---|
| O1 | ✅ | `just run sites-validate` → exit **0**, « 25 site(s) valide(s) ». **25/25** avec `reference_tile` non vide ; `grid.epsg` cohérent avec la zone MGRS (326xx au nord, **327xx pour B06/B07/B08 en hémisphère sud**) ; **50 origines, 0 non multiple de 20 m** (revérifié indépendamment par l'orchestrateur) |
| O2 | ✅ | C07, test **pur** sur entrées littérales : marge 52TDL ≈ 500,4 m vs 52TEL ≈ 4 159,6 m → **52TEL retenue**. ⭐ La règle D-c l'emporte bien sur le décompte d'items, qui aurait donné 52TDL (319 items contre 317) — c'est exactement le défaut de la v3 que D-c corrige |
| O2bis | ✅ | Marges égales (1 000 m), comptes 317 vs 319 → la plus fournie est choisie |
| O3 | ✅ | `TINY_WAE_STAC_URL` injoignable → exit **3** ; `md5sum` de `sites.yaml` **identique** avant/après (`fc674f8d…`), diff vide |
| O4 | ✅ | `just check` vert, aucun test réseau (`--disable-socket` actif) |

**Résultats de référence** : **C07** (Punggye-ri) → `52TEL`, epsg 32652, origine 504120/4572400 —
2 candidates mesurées. **A01** (ITER) → `31TGJ`, epsg 32631, origine 721080/4845740, mono-tuile.

⭐ **Le piège de l'ancrage a été évité** : `config/sites.yaml` conserve ses **11 lignes de
commentaires**, avertissement U+2212 compris (compté avant/après par l'orchestrateur : 11 = 11).
L'édition est textuelle ligne à ligne, jamais un `yaml.safe_dump` du document. Écriture en deux
temps : relecture-validation complète (`load_sites` + `validate_sites`) sur un fichier temporaire
**avant** le `Path.replace` atomique.

**Décisions prises en cours de fiche** : EPSG dérivé du code MGRS par fonction pure testée à sec,
plutôt que lu depuis `proj:epsg` (les deux coïncident — vérifié sur 52TDL/52TEL) ; bbox du relevé
construite dans la zone UTM « naturelle » du point, la tuile de référence étant encore inconnue à
ce stade.

⚠ **Découverte de terrain** : earth-search **rejette** une plage `datetime` au format date seule
(`YYYY-MM-DD/YYYY-MM-DD` → 400) et exige le RFC3339 complet avec heure. Corrigé dès
l'implémentation — à savoir pour toute fiche aval qui interroge `/search` ou `/aggregate`.

⭐ **Contrat de format tranché ici** : `reference_tile` est stocké en **code MGRS nu** (`52TEL`),
pas en `grid:code` préfixé (`MGRS-52TEL`). Conséquence directe pour l0-02.1, dont le filtre tuile
doit normaliser — voir le ré-ancrage de cette fiche.
